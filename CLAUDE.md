<!-- Agent-consumed file. Keep terse, unambiguous, machine-parseable. -->

# Helium

XML toolkit for Go covering XML parsing, SAX2-style streaming, XPath 3.1,
XInclude, XSD, Relax NG, and Schematron. Started as a libxml2-style port to
Go and grew broader native Go APIs and features along the way.

## XPath 3.1 — XSD Version

The xpath3 package targets **XSD 1.1 only**. This means `+INF` is a valid lexical form for xs:double and xs:float, and xs:dateTimeStamp is a recognized type. QT3 tests with `dependency type="xsd-version" value="1.0"` are skipped.

## XSD — Version Toggle

The xsd package defaults to **XSD 1.0** and treats 1.1 as **opt-in** via `Compiler.Version(xsd.Version11)` (or a `vc:minVersion="1.1"` hint on the root `<xs:schema>` when no explicit version is set). The resolved version is frozen onto the compiled `Schema` so the `Validator` applies the same semantics. 1.0 stays the default so existing behavior and goldens are unchanged.

Implemented in 1.1 mode so far:
- the 1.1-only lexical forms (`+INF` for xs:double/xs:float; year `0000` on the date types — both gated in `internal/xsd/value` via a `value.Version` argument; relaxng is pinned to `value.Version10`);
- the 1.1 built-in datatypes (xs:dateTimeStamp, xs:dayTimeDuration, xs:yearMonthDuration, xs:anyAtomicType, xs:error);
- **UPA weakening** (`check_upa.go` `entriesOverlap`: in 1.1 an element competing with a wildcard is not a cos-nonambig violation — the element wins. Element-over-wildcard precedence is ENFORCED at validation for the CHOICE case (`validate_elem.go` `matchChoice`/`tryMatchChoice`, gated on `vc.version == Version11`): a branch that consumes the current child via an element leaf AS ITS FIRST CONSUMING TERM takes precedence over one that would consume it via a wildcard, regardless of declaration order or nesting. The classifier (`particleConsumesViaElement` → `particleFirstConsumerKind`/`groupFirstConsumerKind`) is PATH-AWARE: it respects compositor order, occurrences, and emptiable prefixes (reusing `particleEmptiable`), so a LEADING wildcard inside a sequence (e.g. `sequence(any skip, element a)`) is correctly classified as wildcard-first and does NOT win element precedence even though a later element leaf in the same group also matches — only bounded first-consumer determination, no backtracking. A `skip` wildcard declared before a typed element (directly or as a leading nested term) thus does not steal the element's child or false-accept an invalid value. Selection is COMMIT-NO-FALLBACK: once any branch is element-first for the current child the choice MUST use an element-first branch and never falls back to a wildcard branch, even if the chosen element branch then fails structurally (a later required term missing, e.g. `choice(sequence(a:int, b:int), any skip)` with only `<a>`) or by content. The SEQUENCE case is enforced too (`matchSequence`/`tryMatchSequenceOnce`/`tryMatchSequence` via `sequenceElementReservedLimit`, gated on `Version11`): before matching each particle, the children a LATER element particle in the same sequence is element-first-consumer for are reserved (the particle sees a truncated `children[:limit]`), so a leading minOccurs=0 wildcard (direct or nested as a sub-sequence's first term) does not greedily consume the required named elements. The reservation is REACHABILITY-GATED: it fires ONLY when the current particle would consume the child via a wildcard, not an element leaf (element-vs-element stays in declaration order), AND only for a later element REACHABLE from the current position — every intervening particle is emptiable (`particleEmptiable`) — so a required element between the wildcard and a trailing optional element keeps its child (no over-rejection); bounded lookahead, no backtracking, XSD 1.0 byte-identical. The cos-nonambig position automaton also carries a per-position ORIGIN tag (`firstSetEntry.origin`, `positionAutomaton.nextOrigin`): `applyOccurs` RESETS the counter before each occurrence copy it walks, so all occurrence-copies of ONE textual element leaf (at any nesting depth — e.g. `(a{1,2}){1,2}`) share an origin while two DISTINCT textual leaves get distinct origins even when same-named and even when they share an `*ElementDecl` through a group ref. `entriesOverlap` treats two element positions with the same name but the SAME origin as non-ambiguous (one particle repeated, never a UPA violation), while distinct origins with the same name stay a violation (`choice(a,a)`, `choice(ref g, ref g)`) — this is VERSION-INDEPENDENT (element-vs-element cos-nonambig is identical in 1.0 and 1.1; only element-vs-wildcard weak-wildcard is version-specific), so a deterministic occurrence-copy model like `sequence maxOccurs="100"(a maxOccurs="unbounded")` around a separator (W3C particlesZ034/Z036/V001/M035) is accepted in 1.0 too, not false-rejected by a pure-name comparison. Because the UPA relaxation no longer masks two invalid schemas it used to reject as a side-effect, two guards keep them rejected: `checkAllGroupRef` (`link_refs.go`) rejects a DIRECT `xs:group ref` to an all group whose `{min occurs}` > 1 (cos-all-limited, W3C particlesEa023), and the XSD 1.0 derived-GROUP-restricting-base-ELEMENT path (`restriction_particle.go` `particleValidRestriction`) uses `pointlessReduce` (safe §3.9.6 occurrence hoisting) so a genuinely repeating group is rejected rather than accepted by emission-range language inclusion (W3C particlesHb011; 1.1 keeps `groupRestrictsElement`, unchanged);
- **xs:assert on complex types** (`assert.go`: parsed from complexType/restriction/extension AND simpleContent extension/restriction, pre-compiled via xpath3, evaluated after content validation — EBV false → invalid; inherited down the base chain. `$value` is bound to the element's TYPED simple value for a simpleContent complex type (empty sequence for complex content; the element declaration's default/fixed effective value is substituted for an EMPTY element before typing, so `$value` reflects the schema-normalized value). The test runs against an ISOLATED tree: a deep copy of the element rooted in NO document and stripped of comment/PI nodes, so an absolute path `/`/`//` raises XPDY0050 — the test cannot navigate to ancestors/siblings — while in-scope namespaces — INCLUDING an inherited default namespace (prefix "") so `namespace-uri-for-prefix('', .)` still resolves — are re-declared on the copy and PSVI type annotations are carried onto DESCENDANT elements and ALL attributes (but NOT the assertion-tree ROOT element, whose own type is not yet assigned during its assert, so `data(.)` is untyped — matching Saxon/the conformance tests). A single-schema `xpath3.SchemaDeclarations` adapter (`schema_decls.go`, `schemaDecls`, carrying the schema version) is passed so a node typed with a NAMED user simple type atomizes through its builtin base (e.g. `data(@x) instance of xs:integer`) and a user-defined `cast`/`castable` inside the assertion validates with the schema's version (so a 1.1-only lexical form like year `0000` is castable). The SINGLETON-OPERAND expression evaluators that atomize their operand — `cast`/`castable` (`eval_types.go`), binary arithmetic + unary minus (`eval_arithmetic.go`), and the range `to` bounds (`eval_operators.go`) — atomize THROUGH the typed-value stream via the shared `atomizeSingletonOperand` helper (caps at two atoms) and apply their cardinality requirement to the ATOMIZED result, NOT the raw item count, so a single schema-typed node whose typed value is a list/union expands consistently with `data()`/`$value` (`not(@u castable as xs:string)` is true when `@u`'s typed value is two xs:int; a multi-atom operand to `cast`/arithmetic is an XPTY0004 cardinality error). The helper fast-paths a single already-atomic item (no streaming), so non-schema behavior and hot numeric paths are unchanged; function-call arguments are already handled by signature coercion (`coerceToSequenceTypeE`, which atomizes via `atomizeStream`), and `instance of`/`treat as` do not atomize. BOTH the `castable` AND the `cast` paths (`xpath3/eval_types.go`) are namespace-aware for QName/NOTATION-derived user types — `cast as t:QNameDerived` validates via `ValidateCastWithNS` and returns the namespace-RESOLVED QName/NOTATION value carrying the user type (additive: only when a context-free `CastAtomic` fails and SchemaDeclarations are present, so existing xpath3 cast behavior is unchanged). The `(local, ns)` for that schema lookup is derived from the ALREADY-RESOLVED target type (`Q{ns}local`, via `schemaAnnotationParts`), NOT from re-parsing the lexical prefix, so an UNPREFIXED user type resolved through an `xpathDefaultNamespace` is looked up in the right namespace. When the cast SOURCE is an ALREADY-RESOLVED QName value (e.g. `data(@q)` whose prefix is declared only on the INSTANCE node, absent from the assertion's static namespaces), `qnameCastLexical` re-validates using the value's OWN URI (binding the lexical's prefix to that URI in a copy of the static map) so the cast succeeds instead of failing to re-resolve `prefix:local`. QName/NOTATION atomization (`resolveQNameFromNode`) predeclares the `xml` prefix → the XML namespace without a node binding (matching `xsd.resolveLexicalQName`), so an xs:QName value `xml:lang`/`xml:space` atomizes; and a list-typed node atomizes by splitting on XSD whitespace ONLY (space/tab/CR/LF, via `xsdListFields` — NBSP and other Unicode whitespace are NOT separators, matching validation/`$value`, so a list item containing NBSP stays one token) and atomizing each via `atomizeListTokenAt` (used by `data()`/comparison): when the list's ITEM type is a UNION, each token is atomized through its OWN value-dependently-resolved ACTIVE union member (`nodeItemFor` tokenizes the node value and resolves a per-token leaf into `NodeItem.ListItemLeaves` via `resolveActiveUnionLeafForValue`), so `xs:list itemType="<union>"` agrees with `$value` token-by-token instead of forcing every token through one static base; otherwise it falls back to `atomizeListToken` on the declared item type, where an `xs:QName`/`xs:NOTATION` item type (or a user type whose builtin base is one, carried in `ListItemAtomized`) resolves each token against the node's in-scope namespaces, and a NON-QName USER item type (a `Q{...}` name `CastFromString` can't resolve) is cast through its `ListItemAtomized` builtin base and typed as the user type with that base — so `xs:list itemType="xs:QName"` atomizes (`count(data(@qs))`) AND a list whose item type derives from `xs:int` yields numeric atoms usable by `sum()` (matching `$value`). INLINE ANONYMOUS list/union types (which have no schema-table name, so `xsdTypeName` would collapse them to a named ancestor / `xs:anyType` and lose their item/member metadata) are recorded for assert node atomization under STABLE SYNTHETIC annotation names (`vc.assertAnnotationName`/`assertRegisterAnon`/`assertAnonTypes`/`assertAnonNames`, ns `urn:x-helium:assert-anon`), and `schemaDecls` resolves list-item/union-member metadata from the registered `*TypeDef`, so an inline `xs:list itemType="xs:QName"` or `xs:union memberTypes="xs:QName xs:string"` attribute/element atomizes correctly. The anonymous list-item / union-member types reachable from a list/union are registered RECURSIVELY and for ANY variety — INCLUDING an inline anonymous ATOMIC (faceted) member, e.g. an `xs:int` restricted to `maxInclusive=10` — and regardless of whether the enclosing list/union is itself named, so active-member selection's `ValidateCastWithNS` validates the ACTUAL faceted member (value `"20"` skips that int member on the facet and falls through to a later `xs:string`) instead of a collapsed builtin ancestor. A standalone anonymous ATOMIC node type keeps `xsdTypeName` (its builtin ancestor) so node annotations are not broadly changed. `schemaDecls.IsSubtypeOf` also resolves a synthetic anonymous annotation through the registry (`lookupTypeName`, walking bases via `d.typeName`) and treats EVERY simple type as a subtype of `xs:anySimpleType` (the implicit root, even when a list/union `BaseType` pointer is nil), so an `instance of attribute(v, xs:anySimpleType)` test against an inline anonymous list/union node holds. A UNION node's ACTIVE member is resolved VALUE-DEPENDENTLY during node atomization. `nodeItemFor` precomputes `NodeItem.ActiveUnionMember` (a `*NodeItemUnionMember`, the resolved LEAF, nil when not a union) via `resolveActiveUnionLeaf`: the FIRST direct member, in declaration order, the value FULLY validates against — both the lexical/value cast (and, for a list member, every token plus the list structure; an EMPTY list value of zero tokens is accepted lexically — `unionMemberCastOK` does NOT reject it — leaving any `minLength`-style facet to `ValidateCastWithNS`, so a `union(IntList, xs:string)` value `""` resolves to the EMPTY list and `data(@u)` is the empty sequence/`count`=0, matching `$value`, not the later `xs:string`) AND the member's own facets/assertions via `SchemaDeclarations.ValidateCastWithNS` (a no-op for built-ins, where the cast already covers validity); when that member is ITSELF a union, resolution DESCENDS recursively to the nested leaf, mirroring `fixedUnionActiveMember` so `data()` and `$value` agree for arbitrarily nested unions (e.g. `Outer=union(Inner,xs:boolean)`, `Inner=union(IntList,xs:string)`, value `"1 2"` → leaf `IntList` → two xs:int). The descent terminates via a VISITED-SET cycle guard keyed by union type name (NOT a depth cap), so any finite acyclic nesting — however deep — is fully walked while a cyclic union graph still terminates, matching the validation side's visited-set guards. This matches the `$value` selection — e.g. `memberTypes="ExactlyTwoInts xs:string"` (an xs:list restricted to `xs:length=2`) for value `"1 2 3"` SKIPS the list member on the LENGTH facet and resolves to `xs:string`, not a wrongly-expanded 3-int list. `atomizeUnionItems` (used by both the stream and comparison atom paths) then atomizes through that leaf member: a LIST leaf expands to its per-item atoms (so `memberTypes="IntList xs:string"` value `"1 2"` is two xs:int, `count` = 2); an ATOMIC leaf yields a single atom TYPED AS THE MEMBER (so `data(@u) instance of xs:string` holds). The evaluation ctx is threaded through `nodeItemFor` (not `context.Background()`) so the member validation participates in the cast-guard chain. This activates only when union/list metadata is present (schema-aware atomization), so non-schema-aware xpath3/xslt3 behavior is unchanged. The assert evaluator also sets `Evaluator.QNameValueNoDefaultNamespace()` so an UNPREFIXED xs:QName/xs:NOTATION VALUE atomizes to NO namespace (XSD value-space semantics — a QName value, unlike a name, does not pick up the default namespace; a prefixed value still resolves); under this opt-in `castToQName` ALSO skips the default-namespace fallback for an unprefixed cast target value (e.g. `'bare' cast as xs:QName`), so an unprefixed QName VALUE has no namespace there too — default xpath3 behavior (flag off) is unchanged. A self-referential cast (`cast`/`castable as t:T` inside t:T's OWN assertion) is guarded by a per-validation `(type, value)` cast stack threaded through the context (`castGuardKey` in `schema_decls.go`): a repeat fails closed (treated as not castable) instead of recursing to a stack overflow. Node atomization of an EMPTY DEFAULTED/FIXED DESCENDANT element also sees its schema value: `validateSimpleContent` records each empty element's effective default/fixed value (plus the DECLARATION namespace context for a QName/NOTATION prefix) in `vc.assertEffectiveValues`, and `isolatedAssertTree`/`mapAssertAnnotations` MATERIALIZE it onto the isolated copy (`materializeAssertDefault`/`materializeAssertText` append the value as text and, for a QName/NOTATION default, declare its prefix from the declaration context — mirroring `materializeQNameAttrValue`'s collision handling: if that prefix already binds a DIFFERENT URI in the isolated copy's scope, a fresh prefix is minted, declared to the declaration URI, and the appended text is rewritten to it, so the default resolves against the SCHEMA default-value namespace even when the instance rebinds that prefix), so `data(c)` on `<c/>` declared `xs:integer default="5"` atomizes `5` (and a QName default resolves via its declaration ns), matching the child's own `$value`. The asserted ROOT element is excluded (`$value` already substitutes it; `data(.)` is intentionally untyped); defaulted ATTRIBUTES need no special handling because validation inserts them into the live tree (`SetAttributeNS`). A QName/NOTATION fixed/default value substituted into an EMPTY element resolves its prefix against the DECLARATION's namespace context (`ElementDecl.FixedNS`/new `DefaultNS`, via `effectiveValueNS`), not the instance's, for both validation and `$value`; a materialized QName/NOTATION DEFAULT attribute has its prefix declared on the element (`materializeQNameAttrValue` — whitespace-COLLAPSES the value before extracting the prefix so `default=" p:x "` still binds `p`, resolves a UNION attribute type's ACTIVE member (`fixedUnionActiveMember`) so a `memberTypes="xs:QName xs:string"` default active in its QName member is materialized too, minting a fresh prefix on a conflicting instance binding) so a later xs:assert/IDC atomizes it in the schema value space — gated to 1.1, so XSD 1.0 inserts the default exactly as authored with no namespace-declaration rewrite (byte-identical serialization));
- the **xs:assertion simple-type facet** (`assertion_facet.go`: parsed by `parseFacets`, stored on `FacetSet.Assertions`, evaluated at simple-value validation time with `$value` bound to the TYPED atomic value — a sequence for a list type with each item typed by its active member, a union resolved to its active member FIRST in `buildValueSequence` (so a union whose active member is a LIST, e.g. `memberTypes="IntList xs:string"` with value `1 2`, yields the list-item sequence — two xs:int — not one untypedAtomic) (SCHEMA-AWARE: `typedAtomic`/`buildValueSequence` thread `vc.schema` into `fixedUnionActiveMember` so a member whose own assertion needs `castable as t:T` is selectable, matching the real validation path — `fixedValueMatches`/`fixedUnionMatches`/`crossMemberValueEqual` also thread `vc.schema` (`c.schema` at compile time) so fixed/enumeration comparisons resolve the active member in the right value space), a QName/NOTATION lexical resolved against in-scope namespaces into an `xpath3.QNameValue` — built by `buildValueSequence`/`typedAtomic`/`atomicForType` — which PRESERVES a NAMED user-defined type's identity (stamps the user type name as `TypeName`, builtin cast type as `BaseType`, mirroring `AtomizeItem`/`data()`) across the atomic, QName/NOTATION, list-item, and union-leaf paths, so `$value instance of t:MyInt` holds and `$value` agrees with `data()` on type identity; the context item is ABSENT, so `.`/`position()`/`last()` raise a dynamic error → unsatisfied; assertions ANDed along the restriction chain; same SchemaDeclarations adapter passed. Because `validateValue` can evaluate these assertion facets, the COMPILE-TIME throwaway contexts that reach it — the attribute default/fixed constraint check (`checkAttrUseConstraints`, `link_refs.go`) and the facet value-against-base / enumeration / union-member checks (`check_facets.go`) — are built with `schema: c.schema`, so a schema-aware cast in a facet assertion doesn't fail closed and reject a VALID schema at compile time. The runtime `typeAcceptsValue` (IDC field) and standalone `TypeDef.Validate` remain on the Version10/nil-schema default — a documented Phase-1 gap); `xpathDefaultNamespace` on xs:assert/xs:assertion and the root `<xs:schema>` (`resolveXPathDefaultNS` whitespace-COLLAPSES the raw value — element-local OR schema-level — before the empty check and the sentinel switch, so `" ##targetNamespace "` resolves like the sentinel rather than a URI literal; `##targetNamespace`/`##defaultNamespace`/`##local` resolved, chameleon-include aware; an IMPORTED schema's root value is carried into the import sub-compiler (`compile_imports.go` sets `impC.schemaXPathDefaultNS`) so imported assertions resolve their own default namespace);
- XSD 1.1 **xsi: attribute references** (`validate.go` `validateAttributes`: an xsi: processor attribute — xsi:type/xsi:nil/xsi:schemaLocation/xsi:noNamespaceSchemaLocation — referenced explicitly as an attribute use (with ANY use: optional/required/PROHIBITED) participates in ordinary attribute-use validation instead of being skipped as an always-special attr, so a `<xs:attribute ref="xsi:type" use="required">` is satisfied by a present xsi:type and reported missing when absent, and a `use="prohibited"` declaration REJECTS a present xsi:type (the declaration is detected via BOTH the allowed and prohibited use maps). The special handling applies ONLY to the FOUR real xsi: processor attributes (`isKnownXsiProcessorAttr`: xsi:type/xsi:nil/xsi:schemaLocation/xsi:noNamespaceSchemaLocation) — the XSI namespace is reserved to exactly these, so a declared ref to any OTHER xsi: local name (e.g. xsi:foo) is NOT specially accepted: it stays skipped as a special attribute, so a required use of it is never satisfied and the instance is rejected. A present non-prohibited declared xsi: use also VALIDATES its value against the attribute's fixed built-in type (`validateDeclaredXsiAttrValue`: xsi:type→a non-empty, namespace-resolvable xs:QName; xsi:nil→xs:boolean; xsi:schemaLocation→a non-empty even list of xs:anyURI tokenized on XSD list whitespace ONLY via `value.XSDFields` (NBSP is not a separator); xsi:noNamespaceSchemaLocation→xs:anyURI), since `ref="xsi:*"` resolves to no typed global attribute — so xsi:type="" / a malformed QName / a non-boolean xsi:nil is a validity error that does NOT satisfy a required use. A declared xsi: use is also ASSOCIATED with its built-in SCALAR type in the attribute-ref resolution loop (`link_refs.go`; `xsiProcessorAttrBuiltinType`: xsi:type→xs:QName, xsi:nil→xs:boolean, xsi:noNamespaceSchemaLocation→xs:anyURI) — `ref="xsi:*"` resolves to no global attribute, so without this its fixed/default would have no type — so a fixed/default constraint is VALIDATED for validity at compile time (`checkAttrUseConstraints`: `ref="xsi:nil" fixed="notbool"` is a schema error) and COMPARED in VALUE space at runtime (`fixedValueMatches`: instance `xsi:nil="1"` satisfies `fixed="true"`; a different-prefix/same-namespace xsi:type satisfies a QName fixed). The instance value itself is validated ONCE — by `validateDeclaredXsiAttrValue` — and the generic type-based value check is skipped for the declared xsi use (`declaredXsiValueChecked`) to avoid double-validation; the fixed-value comparison still runs against the built-in type. xsi:schemaLocation (a LIST of xs:anyURI) has no scalar built-in, so it gets no type association; instead its instance/even-pair value validity stays with `validateDeclaredXsiAttrValue` and its fixed/default is given the SAME value-space treatment via the list helpers (`validateXsiSchemaLocationValue` / `xsiSchemaLocationValueEqual` / `xsiSchemaLocationTokens`, XSD-whitespace-collapse + `value.XSDFields`): the fixed/default LITERAL is VALIDATED at compile time (`checkAttrUseConstraints` — a non-empty EVEN list of xs:anyURI; an odd/empty literal is a schema error) and the runtime fixed COMPARISON is by TOKEN-LIST equality (`isDeclaredXsiSchemaLocationUse` routes around `fixedValueMatches`), so `" urn:a   loc.xsd "` satisfies `fixed="urn:a loc.xsd"`. (helium's xs:anyURI lexical space is unrestricted, so the per-token anyURI check never fails in practice — even/non-empty is the only reachable literal-validity constraint.) Undeclared/implicit xsi processing is unchanged — Saxon Complex complex009/complex010; gated Version11, 1.0 byte-identical);
- XSD 1.1 **attribute inheritance** (`finalizeEffectiveAttrs`: a complex type inherits every base attribute use it does not redeclare, computed for ALL 1.1 derived types — `checkRestrictionAttrs` does not flag a non-redeclared required base attribute as "missing" in 1.1, so the merge is what keeps that inherited requirement enforced rather than silently dropped. The merge is TOPOLOGICAL across BOTH extension AND restriction derivations — base finalized first via memoized, cycle-guarded recursion, then `checkRestrictionAttrs`/`checkExtensionAttrDuplicates` runs against td's OWN declarations and the finalized base, then td inherits — so an extension of a restriction (or a restriction of an extension, at any depth) reads a complete base attribute set regardless of source order. In 1.1 the extension and restriction passes DEFER all attribute work to `finalizeEffectiveAttrs`; XSD 1.0 keeps the original per-pass copy, byte-identical) plus
- **simpleContent content-type narrowing** (`TypeDef.ContentSimpleType` derived from a nested `<xs:simpleType>` AND/OR the restriction's direct facets — `parseSimpleContentRestrictionType` COMPOSES them, returning a restriction of the inline type carrying the sibling facets when both are present, so neither set is dropped — and RECORDS each synthetic restriction in `typeDefSources` so `checkFacetConsistency` runs facet applicability/value checks on it (an inapplicable sibling facet, e.g. `xs:minInclusive` on an `xs:string` base, is a compile error); the EFFECTIVE content simple type is composed across the WHOLE simpleContent derivation chain by `effectiveContentSimpleType`, which recurses through simpleContent complex types (marked `TypeDef.IsSimpleContent`) and stops at the underlying simpleType/builtin, re-basing a facet-only narrowing on the effective base content type so ancestor and derived facets compose. `validateSimpleContent` validates the text against it whenever any chain step has facets/assertions or a non-string builtin (`simpleContentNeedsValidation`), so a restriction enumeration / `xs:float` narrowing is enforced AND inherited through a further restriction or extension, and a simpleContent EXTENSION of a named faceted simple type still enforces that base type's facets/assertions; `$value` uses the same composed type. The assert NODE-atomization adapter `schemaDecls` ALSO resolves a simpleContent COMPLEX type through `effectiveContentSimpleType` (via `lookupAtomizationType`) in its atomization-metadata accessors — `LookupSchemaType`, `ListItemType`, `UnionMemberTypes` — so `data(c)` on a DESCENDANT element whose simpleContent content is narrowed to `xs:QName` / a list / a union atomizes through the NARROWED content type (QName value-space, list items, union active member), agreeing with `$value`, instead of through the complex type's raw base; `IsSubtypeOf`/`validateCast` keep the raw type (a simpleContent type is still a COMPLEX type for node/subtype tests). `UnionMemberTypes` further resolves variety and members via `resolveVariety`/`resolveUnionMembers` (which walk the base chain) rather than the type's direct fields, so a synthetic facet-only restriction over a union base — e.g. a simpleContent restriction `base="u"` (u a union) with a direct `xs:pattern`, whose `effectiveContentSimpleType` synthetic carries only `BaseType` — still reports its union members and resolves the active list member for `data(c)`. A NESTED `<xs:simpleType>` content type is returned as-is (its OWN base chain, not re-based), so `validateSimpleContent` ALSO validates the value against `effectiveContentSimpleType(td.BaseType)` whenever `td.ContentSimpleType.BaseType != td` — a nested simpleType RESTRICTS, it does not REPLACE, the base content type (§3.4.2.2), so an inherited base `maxLength` still rejects an over-long value the nested type alone would accept. FIXED-value comparison narrows a simpleContent type to its content simple type CENTRALLY inside `fixedValueMatches` (gated on `version == Version11`: `td = effectiveContentSimpleType(td)`), so EVERY caller is consistent — the runtime non-empty element fixed check, the attribute fixed checks, AND the COMPILE-TIME content-model restriction check (`restriction_particle.go` NameAndTypeOK, where a base/derived element `fixed` must be value-space equal). A simpleContent fixed value is thus compared in its NARROWED content simple type — e.g. a content type restricted to `xs:QName` accepts a different-prefix/same-URI value by QName value-space equality instead of a lexical comparison against the outer complex type's base chain (a non-simpleContent type passes through unchanged, so attribute/list-item/union-member callers are unaffected; XSD 1.0 keeps the declared-type comparison, byte-identical). XSD 1.0 keeps its per-type gating, byte-identical); and
- **conditional type assignment** (`alternative.go`: `<xs:alternative>` on an element declaration selects the governing type via the first @test that holds against the element, else a testless default, else the declared type; xsi:type still takes precedence; applied at the root AND every per-child match site INCLUDING wildcard-matched lax elements (`validateWildcardChild`) and xs:anyType/lax descendants — at those sites CTA selects the governing type FIRST, then the cvc-elt.4.3 derivation-block check and the idc lax-assessment/ID-pass run against the SELECTED type. Each `<xs:alternative>` may carry an INLINE anonymous `<xs:complexType>`/`<xs:simpleType>` (parsed by `parseTypeAlternative`; a present-but-empty `@type` is itself a governing-type source, so `@type=""` plus an inline type is a conflict and a bare `@type=""` is an invalid QName), `type="xs:error"` (a selected xs:error invalidates the element even through the xsi:nil nilled-element path), and `@xpathDefaultNamespace` (`effectiveXPathDefaultNS`: locally-present value resolved against the alternative, else inherited from the schema-level default; affects only unprefixed element name tests in @test). The @test runs against a DETACHED CTA context node (`inherited_attrs.go` `ctaContextNode`: an orphan element bearing the instance element's own attributes PLUS inheritable attributes contributed by ancestors (`inheritedAttributes`, attributes whose declaration is `inheritable="true"`), with its in-scope namespaces, position()=last()=1, an empty `emptyCollectionResolver`, and the schema base URI — no children or parent). A non-default `@type`/inline alternative must be VALIDLY SUBSTITUTABLE for the declared type (`checkAltSubstitutability` at compile time): `strictBuiltinAwareDerivedFrom` (`builtin_hierarchy.go`) accepts a genuine derivation via `isDerivedFrom` (user types), the built-in simple-type hierarchy via the `builtinSimpleBase` table (the 1.0 built-ins are NOT BaseType-linked), or the anySimpleType simple-content rule; a union declared type also admits any of its (base-chain-resolved) member types via `resolveVariety`/`resolveUnionMembers`; there is NO permissive simple-vs-simple fallback. The block check is built-in-aware (`isDerivationBlocked`). Per-document `xpathDefaultNamespace`/base-URI and the `altTypeRefs`/`ctaElems` worklists are saved/restored across include/redefine and seeded onto import sub-compilers (`compile_imports.go`)); and
- **open content** (`opencontent.go`: `<xs:openContent>` interleave/suffix — interleave seeds the DECLARED sub-sequence with every child matching a declared ELEMENT NAME or a declared WILDCARD particle (modelGroupWildcardAdmitsName — the declared content model may itself contain xs:any, and such a child must be able to satisfy that wildcard's occurrence rather than being forced to open), the rest matching the open wildcard go to open, then it matches the declared model on the declared set; suffix matches the declared model (which the real matcher resolves including declared wildcards) then requires every trailing child to match the wildcard. Interleave is REFINED to the §3.4.4.3.2 existential partition (`refineInterleavePartition`): a declared-seeded child the model cannot place at its position — either a STRUCTURAL blocker (an extra occurrence after a bounded element/wildcard particle is exhausted, found at the trial's `consumed` index) OR a child a declared STRICT/lax WILDCARD structurally CONSUMED whose CONTENT validation then FAILED (the matcher counts it as consumed, so the trial errors with `consumed` already at the end; refineInterleavePartition then moves the LAST open-eligible NON-declared-name child — a declared element NAME stays for element-precedence reporting, never spilled) — is moved into the open sub-sequence when it ALSO matches the open wildcard — found by TRIAL-matching the declared set with diagnostics suppressed (`suppressDepth++`), then re-running for real on the resolved split, so open content and declared content may match the same names. Suffix open content inside an `xs:all` uses a LENIENT member matcher (`matchAll11(...lenient=true)` via `matchContentModelSuffix`) that stops at the first non-member child instead of erroring so the trailing children validate as open content.
- **Schema-level `<xs:defaultOpenContent>`** (`readDefaultOpenContent`) is PER-DOCUMENT (captured on each complex type lacking an explicit `<xs:openContent>` as `TypeDef.pendingDefaultOpenContent`, saved/reset/restored across xs:include/xs:redefine/xs:override/import like the form defaults — an xs:override/xs:redefine REPLACEMENT child uses the OVERRIDDEN/redefining document's default per §4.2.5, threaded back via `overrideLoadTarget`'s returned `*OpenContent`); it must appear before any component declaration (else a schema error), carries mode interleave|suffix (NOT none) and `appliesToEmpty` (default false). The `@mode` value is whitespace-COLLAPSED before the enum comparison (xs:token value space — `normalizeWhiteSpace(.,"collapse")`, in BOTH parseOpenContent and readDefaultOpenContent) so `mode=" suffix "` is accepted; `appliesToEmpty` already collapses via readBooleanAttr/parseSchemaBool and the `<xs:any>` processContents via readProcessContents. The EFFECTIVE {open content} of every complex type is resolved BASE-FIRST by `resolveOpenContent`/`computeEffectiveOpenContent` (in `resolveRefs`, after content models/types are finalized): the explicit `<xs:openContent>` (mode="none" → absent, suppresses the default) OR the per-document default (applied unless the effective content type is empty and appliesToEmpty=false — `contentTypeEmptyForOpenContent`); for an EXTENSION it inherits/merges the base's open content (§3.4.2.2: a base interleave mode wins, wildcards union; an extension may not relax a base interleave to suffix — §3.4.6.2); for a RESTRICTION its validity against the base is checked (§3.4.6.4 `checkOpenContentRestriction`: may not ADD open content to a closed base, derived wildcard must be a subset, derived processContents at least as strong, mode may differ only when the base is interleave — for an EMPTY derived content model only the MODE comparison is waived, while a base that lacks open content but whose declared content model carries an admitting wildcard, e.g. converting a base xs:any particle into open content, still permits the restriction via baseModelAdmitsOpenContent — a CONSERVATIVE-SOUND, fail-closed rule that accepts ONLY the single PROVABLY-SOUND shape (the full content-model restriction-subsumption + weak-wildcard-attribution problem is NOT approximated): the base content model must contain EXACTLY ONE wildcard particle W and NO element-declaration particles anywhere (baseModelWildcardScan.walk), W's OWN occurrence must be minOccurs=0/maxOccurs=unbounded, AND every ANCESTOR group on W's path must be at-most-once (maxOccurs=1) — so the base content language is EXACTLY W* (every count in [0,∞)) and the derived open content is a subset iff wildcardConstraintSubset(derived,W) holds with derived processContents at least as strong. A min/max occurrence PRODUCT of 0..unbounded is NOT sufficient (e.g. sequence(0,unbounded){any(minOccurs=2)} accepts {0}∪[2,∞), a gap at 1), so W must itself be the sole source of repetition. ANY EMITTING declared element in the base disqualifies it (a name-colliding child is ATTRIBUTED to that element and validated more strictly than open content, so the derived would not be a subset — even an EMPTIABLE element); a NON-EMITTING element (effective maxOccurs 0 — own or ancestor-group maxOccurs=0) emits nothing and does NOT disqualify the wildcard-only shape (baseModelWildcardScan.walk threads an emitting flag, skipping non-emitting elements AND wildcards); a second wildcard, a required/bounded wildcard, or any repeating ancestor group are all conservatively deferred. open022 (single optional unbounded wildcard, no elements) accepts; the declared-emptiable-sibling repro and the shared (G,G)* group-ref case (two wildcards) reject. A restriction must ALSO not DROP a base element declaration and re-admit the same name through an open-content wildcard that loses a constraint the base imposed (checkOpenContentDropsBaseLocal). Two unsoundness sources: TYPE loss — a dropped name re-admitted by an UNENFORCING wildcard (processContents=skip, never assessed; or lax with NO global declaration) escapes the base type → rejected (strict, or lax-with-global, resolves a governing type the DYNAMIC wildcard-EDC enforces, so type-DEFINITION-safe); CONSTRAINT loss — on the ENFORCED (strict/lax-with-global) path, the re-admitting GLOBAL must be at least as restrictive as the dropped base LOCAL (localElementDeclsByName, refs excluded) on the SIX properties the dynamic EDC does NOT enforce, all checked by ONE shared `globalDropsLocalConstraint` (used by BOTH the dropped-local and kept-narrowed branches so they cannot diverge): a CTA {type table} NOT EQUIVALENT to the global's (typeTablesConsistent, absent-both=equivalent — the dynamic EDC checks only type-DEFINITION substitutability, not table equivalence; note the dropped-local skip-base case is the only INDEPENDENTLY-reachable type-table cell — a kept-narrowed type-table divergence is already caught by the static wildcard-EDC + the restriction-validity check, since a restriction cannot change an element's type table, so the kept-path type-table check is defense-in-depth), a base-local `fixed` value the global lacks or differs on in VALUE space (compared via fixedValueMatches with each declaration's FixedNS namespace context, NOT lexically — so a QName/NOTATION value with the same lexical form but a different prefix→namespace binding is DIFFERENT, while a numeric/list value differing only lexically like "1.0" vs "1.00" is EQUAL), a global that is `nillable` while the local is not, a base-local identity constraint (xs:key/unique/keyref, matched by resolved QName) the global does not impose, a `block`/{disallowed substitutions} the local sets but the global does not (`local.Block &^ global.Block != 0` — validateWildcardChild applies the GLOBAL's block, so a base-local block="#all" rejecting an xsi:type derivation is lost), or — ASYMMETRICALLY — a `default` the GLOBAL supplies but the local does NOT or differs on in VALUE space, DefaultNS-aware (`global.Default != nil && (local.Default == nil || !fixedValueMatches(...))` — an empty element re-admitted via the global gets the global's default substituted, validating content the base rejected; a BASE-LOCAL default forbids nothing, so it is not compared) → reject — (a base ref/substitution member resolves to the SAME global in both, so no divergence; the kept-local-narrow case — where the derived KEEPS bn but narrows its maxOccurs so excess spills — is caught separately in the kept-name branch below); ORDER loss — a SUFFIX-mode BASE requires a declared name in the prefix region, so dropping it lets the derived accept it as trailing open content regardless of processContents (the dynamic EDC enforces TYPE not ORDER) → a dropped name a suffix base declares is rejected for ANY processContents (strict included), an interleave base imposes no ordering. BUT the guard fires only when the derived open content would ACTUALLY ACCEPT the dropped name: skip/lax accept it, but a STRICT wildcard accepts it ONLY with a matching global declaration — strict-WITHOUT-a-global REJECTS the child (no spill), so the derived is MORE restrictive and the drop is EXEMPT (do not reject). A base name the derived KEEPS as a declared element needs OCCURRENCE COVERAGE only in INTERLEAVE mode with an UNENFORCING wildcard: the EXCESS children beyond the derived particle's maxOccurs spill into the interleave open content (refineInterleavePartition); if the derived narrows below the base's effective maxOccurs (maxOccursForName, summing sequence/all members, max over choice branches, folding group maxOccurs, counting substitution members; occursCovers: unbounded base needs unbounded derived) the spill is unsound TWO ways: an UNENFORCING wildcard loses the declared TYPE entirely → REJECT; an ENFORCING wildcard (strict/lax-with-global) type-checks the spill but via the GLOBAL declaration, LOSING the base local's fixed/nillable/identity constraints → REJECT when the global is not constraint-compatible with the base local (the SAME globalDropsLocalConstraint check as the dropped-local case; a strict wildcard WITHOUT a global rejects the spilled child, so no global means no spill and the check is skipped). When the derived COVERS the base occurrence no excess spills, so it is safe regardless of enforcement. SUFFIX mode is always safe for a kept name (a trailing declared-name child is rejected as misplaced, never spilled). The guard protects every name the base content model ADMITS at runtime (baseAdmissibleElementNames mirrors elemMatchesDeclOrSubst): each element particle's OWN name when concrete (a LOCAL declaration or a ref-to-global) PLUS its INSTANCE-ADMISSIBLE (abstract-excluded) transitive SUBSTITUTION-GROUP members — so a derived wildcard that excludes the head via notQName="h" but still re-admits a member m loses m's type under skip just as it would the head's. A skip wildcard returns before any global lookup so it never enforces the type (any admitted name); a ref/member under lax is always exempt (a global exists, so lax resolves it). An abstract head's own name is excluded (no instance bears it). A NON-EMITTING base name — effective base maxOccurs 0 (its particle or any ANCESTOR group is maxOccurs=0/prohibited, detected by maxOccursForName==0, folding ancestor groups via mulOccurs) — is filtered out BEFORE the dropped/kept split (the base admits it only via open content, not the element, so dropping it while keeping the same open content is valid). The DERIVED kept-name set and SUFFIX validation's misplaced-trailing-name set both use collectEmittingModelElementNames — restricted to EMITTING particles AND to names elemMatchesDeclOrSubst can actually consume (the particle's own name when CONCRETE plus its INSTANCE-ADMISSIBLE, abstract-EXCLUDED substitution members, via instanceSubstMembers, mirroring baseAdmissibleElementNames) — so a derived element narrowed to maxOccurs=0 counts as DROPPED (it spills to open content), a non-emitting declared name in the suffix region is open content, and an ABSTRACT head/member name (which can never appear as declared content) in the suffix region is open content too, not a misplaced declared element. (The INTERLEAVE name-split keeps collectModelElementNames, abstract-included, but is sound: the matcher cannot consume an abstract member, so refineInterleavePartition moves it to open.) At RUNTIME, validateContentModelOpen PRUNES non-emitting particles from the matched model (pruneNonEmittingParticles, removing maxOccurs=0 particles, whole maxOccurs=0 groups, AND any group that becomes EMPTY after pruning its children — propagating up, so an all-prohibited xs:choice/group is dropped rather than left as an empty group whose retained minOccurs makes the matcher report a missing branch. The prune is SEMANTICS-PRESERVING for xs:choice EMPTIABILITY: an empty branch — intrinsically empty `<xs:sequence/>` or a group emptied by pruning a maxOccurs=0 member — makes the whole choice emptiable, so silently dropping it would turn `choice(<empty>, g)` into `choice(g)` and falsely REQUIRE g. In a SEQUENCE/all parent an emptied member is a no-op and is dropped (a required sibling stays required); in a CHOICE parent the emptied branch is REPLACED by a normalized emptiable empty-SEQUENCE particle (minOccurs 0) so the choice still matches the empty string — never a literally-empty choice the matcher treats as missing. A DIRECT prohibited element leaf `choice(e{0,0}, g)` is dropped outright with no replacement — g stays required — because the empty-branch handling fires only on a GROUP term that prunes to empty, not on a maxOccurs=0 leaf caught by the top-level drop) at its top BEFORE matching, so a DIRECT prohibited particle cannot consume a child via the matcher (matchElementParticle grabs one child before its maxOccurs check) — the child falls through to open content in BOTH suffix and interleave. A fully non-emitting declared model prunes to zero particles; validateContentModelOpen then NORMALIZES it to an empty sequence (minOccurs=0) so the matcher matches nothing (no missing-branch error) and every child routes to open content. This makes baseModelAdmitsOpenContent (scan emitting flag), the drop guard (maxOccursForName/collectEmitting), suffix + interleave validation, AND the runtime matcher one consistent non-emitting definition; scoped to the Version11 open-content path (validateContentModelOpen), so the ordinary matcher is byte-identical. (materializeOpenContentSiblings, called from computeEffectiveOpenContent on the LOCAL effective open content BEFORE any extension union / restriction check / base inherit, populates SiblingNames on an open-content/defaultOpenContent wildcard carrying ##definedSibling — the element names declared in the type's own content model — CLONING the wildcard first since a default/explicit wildcard is shared across types. wildcardExcludesName checks SiblingNames not the marker, so without this an open-content ##definedSibling wildcard would exclude nothing and a declared sibling could be moved into open content by interleave refinement. It must run BEFORE the extension union (wildcardUnion/cos-aw-union computes the union's SiblingNames from the OPERANDS' SiblingNames, retaining a sibling neither admits) so a one-sided union — one operand notQName/namespace-excludes the sibling, the other ##definedSibling-excludes it — still retains the sibling as a finite exclusion even when the live marker is folded away; the base operand is materialized BASE-FIRST.) A name the derived wildcard EXCLUDES (notQName/##defined) is never re-admitted, so exempt; xsi:type is instance-driven (dynamic EDC), not a compile-time name source. The dropped-name rule keys on the BASE's open-content mode (suffix→reject any pc; interleave→reject only if unenforcing); the kept-name coverage keys on the DERIVED's mode (interleave-only). Suffix is the only open-content mode imposing an ordering constraint. The SYMMETRIC guard checkOpenContentDropsBaseWildcard protects dropped/narrowed base DECLARED WILDCARD particles the same way (a declared xs:any wins attribution — interleave seeds it to declared via modelGroupWildcardAdmitsName, suffix matches it in the prefix; a suffix leftover declared-WILDCARD match is NOT flagged misplaced — that check only catches element NAMES — so it still spills to open). For each emitting declared base wildcard bw (collectEmittingDeclaredWildcards, with effective max) whose namespace the derived OPEN wildcard ACTUALLY re-admits (constraintsIntersect FIRST — an open content that excludes bw's namespace cannot receive its excess, so exempt; AND, for a STRICT open, strictOpenAdmitsGlobalIn — a strict open with NO globally-declared element in bw's region admits nothing there, so no spill → exempt) AND where the open does not enforce at least as strictly (interleave: open pc weaker than bw; SUFFIX: ANY pc — suffix is NOT blanket-exempt, it spills too), check AGGREGATE occurrence coverage over the SPILL-RELEVANT REGION = bw's namespace ∩ the derived open-content wildcard's namespace (spillCon = constraintIntersect(bwCon, openCon); spillWC = constraintToWildcard(spillCon)) — only names the open content ADMITS can leave the declared model and spill, so names bw admits but the open EXCLUDES need no derived coverage: baseCap = sum of effMax over base wildcards that INTERSECT the spill region with pc>=bw's; derivedCap = sum over derived wildcards that namespace-SUPERSET the spill region (wildcardConstraintSubset(spillWC, dw)) with pc>=bw's; if !occursCovers(derivedCap, baseCap) reject (suffix=order loss / interleave=type loss). The accounting over-estimates baseCap (overlap) and under-estimates derivedCap (subset), so it is fail-closed toward soundness; UPA forbids two overlapping declared wildcards in one content model, so the aggregate reduces to single-wildcard coverage for constructible schemas (the multi-wildcard aggregate scenario is not constructible). Both guards run in the restriction branch, so a base declaring BOTH an element and a wildcard is covered. A THIRD restriction guard checkDerivedWildcardReadmitsBaseOpen covers QUADRANT B (derived DECLARED wildcard re-admits BASE OPEN content): a restriction may DROP the base's open content O and re-introduce its language as a declared wildcard (restriction_particle.go's Wildcard-restricting-ModelGroup conservatism lets that compile), and a declared wildcard WINS attribution, so its processContents governs — not O's. For each emitting derived declared wildcard whose namespace intersects O, unless it VALIDLY RESTRICTS a base DECLARED wildcard (W_d ⊆ W_b, pc≥ — that is quadrant D, restriction_particle.go), it must be a valid restriction of O itself: for an INTERLEAVE O, namespace ⊆ O AND processContentsStrength(W_d) ≥ O's (else REJECT — a skip/weaker or broader-namespace wildcard cannot re-admit what the base strict/lax open content validated); for a SUFFIX O the trailing-ORDERING constraint cannot be preserved by a (non-trailing) declared wildcard, so re-admitting O's namespace as a declared wildcard is REJECTED — BUT only when the derived wildcard ACTUALLY admits a child there: skip/lax always admit, while a STRICT derived wildcard with NO matching global element in the intersecting region admits nothing (strictOpenAdmitsGlobalIn) so no order is lost → EXEMPT, mirroring the quadrant-A strict/no-global exemption. A base WITHOUT open content is unaffected. The four base-vs-derived content-interaction quadrants for restriction subset are thus: (A) derived OPEN re-admits base DECLARED → checkOpenContentDropsBaseLocal/Wildcard; (B) derived DECLARED wildcard re-admits base OPEN → checkDerivedWildcardReadmitsBaseOpen; (C) derived OPEN vs base OPEN → checkOpenContentRestriction; (D) base DECLARED vs derived DECLARED → restriction_particle.go. SUFFIX base-mode ORDERING is preserved in every quadrant: A (drop guards reject a dropped suffix-declared name / spilled suffix wildcard for ANY pc), B (this guard rejects a derived declared wildcard re-admitting a suffix base open content), C (checkOpenContentRestriction rejects a mode change off a suffix base — base suffix → derived must stay suffix; an interleave base may not be extended to suffix). (D's Wildcard-vs-ModelGroup case is conservative — a derived declared wildcard re-admitting a base declared ELEMENT's name under skip/weaker validation is a RESIDUAL not covered by B, which only checks against base OPEN content). The `<xs:any>` of an open content must NOT carry minOccurs/maxOccurs (`parseOpenContentWildcard`), an open-content element (xs:openContent OR xs:defaultOpenContent) has a STRICT (annotation?, any?) child grammar (scanOpenContentChildren/reportOpenContentChildGrammar): at most one annotation, at most one xs:any, the annotation must PRECEDE the any, and any stray (non-annotation/non-any) child is a schema error; mode="none" must not carry a wildcard child; and a complex type must not have more than one xs:openContent (duplicate siblings rejected in all three parse loops). VALIDATION enforces effective open content on a type with NO declared model group too (validate.go: a mixed-or-appliesToEmpty type that picked up a defaultOpenContent does not admit arbitrary children via annotateAnyTypeChildren — every child element must match the open wildcard, mixed text still allowed), so all four ContentType branches enforce td.OpenContent (simple content never carries it). CHILD ORDER (xsd.org §3.4.2, openContentOrderViolation, all three complexType parse loops): an xs:openContent must PRECEDE the content-model particle, the attribute uses, the anyAttribute wildcard, and the trailing xs:assert region, and must NOT be a sibling of an xs:simpleContent/xs:complexContent wrapper (the direct complexType grammar is a CHOICE — either the wrapper branch (simpleContent|complexContent) or the non-wrapper branch (openContent?, particle?, attrs, anyAttribute?, assert*) — so EVERY non-wrapper-branch child kind is mutually exclusive with a wrapper in BOTH orders: model group, attribute/attributeGroup, anyAttribute, openContent, AND xs:assert (a wrapper rejects a prior assertSeen, and the assert case rejects a prior contentWrapperChild; assert is 1.1-only so the guards never affect 1.0)); xs:openContent is also rejected INSIDE a simpleContent extension/restriction (parseSimpleContentChildren — simple content has no element content for open content to apply to), and BOTH `<xs:complexContent>` and `<xs:simpleContent>` share the `(annotation?, (restriction | extension))` content model, enforced by one shared helper `parseDerivationWrapper` in BOTH XSD 1.0 and 1.1 (this grammar is version-INDEPENDENT): EXACTLY ONE derivation, an annotation only BEFORE it, nothing else — a ZERO derivation, a SECOND derivation, an annotation AFTER the derivation, or any stray child BEFORE/AFTER it (notably a trailing `<xs:openContent>`, which belongs INSIDE the wrapper) is a schema error. The check lives in the OUTER `parseSimpleContent` (not just `parseSimpleContentChildren`), so a stray/second/trailing child directly under `<xs:simpleContent>` is caught. For a VALID wrapper (one derivation) the dispatch is identical to the old lenient loops, so goldens stay byte-identical; only the invalid forms are now rejected in 1.0. The direct complexType `assert*` trailing region is also enforced: a content-model particle, attribute, attributeGroup, or anyAttribute appearing AFTER an `<xs:assert>` is out of order (assert is 1.1-only, so the `assertSeen` guards never affect 1.0). The DERIVATION-BODY grammar (the children of an `<xs:restriction>`/`<xs:extension>`) is enforced too, in BOTH XSD 1.0 and 1.1 (it is version-INDEPENDENT): the complexContent body `(annotation?, openContent?, (group|all|choice|sequence)?, ((attribute|attributeGroup)*, anyAttribute?), assert*)` is parsed by ONE shared `parseComplexContentDerivationBody` (parseRestriction/parseExtension are thin wrappers over it, kind-parameterized for the extension prohibited-attr warning) enforcing annotation first/at-most-one, single model group before attributes, attribute before anyAttribute, single anyAttribute, NOTHING after the trailing `assert*` (model group/attribute/attributeGroup/anyAttribute after an assert is rejected), and reject-stray; the simpleContent bodies in `parseSimpleContentChildren` enforce restriction `(annotation?, simpleType?, facet*, (attribute|attributeGroup)*, anyAttribute?, assert*)` and extension `(annotation?, (attribute|attributeGroup)*, anyAttribute?, assert*)` — simpleType at-most-one and before facets, facets/simpleType only in restriction (`isSimpleTypeFacetElement`), attributes after facets, single trailing anyAttribute, nothing after assert, reject-stray. The openContent/assert body cases stay Version11-gated (they are 1.1 constructs), so in XSD 1.0 an `<xs:openContent>`/`<xs:assert>` inside a body is a stray child — genuinely invalid in 1.0. The pre-existing model-group/attribute/facet LEAF parsers are untouched — only child ORDER/CARDINALITY is enforced. The DIRECT `<xs:complexType>` child grammar enforces the ANNOTATION rule version-INDEPENDENTLY (at most one, and it must be the FIRST child — a second annotation or one after any content is a schema error in 1.0 and 1.1). Its stray-rejecting default (only annotation, simpleContent, complexContent, openContent, a model-group particle (group|all|sequence|choice), attribute, attributeGroup, anyAttribute, and assert allowed — any other child, e.g. a direct `<xs:element>`, is a schema error) stays Version11-gated. XSD 1.0 keeps the prior lenient behavior only for that direct stray-child default; a schema-level xs:defaultOpenContent must FOLLOW the composition elements (include/import/redefine/override), so a composition element after it is a schema error. `<xs:complexContent>`/@mixed is honored version-INDEPENDENTLY and must not conflict with `<xs:complexType>`/@mixed, and an extension's content type and its base's must both be mixed or both element-only (§3.4.6.2 cos-ct-extends) — enforced in both 1.0 and 1.1. A global `<xs:complexType>`/@name must be a valid `xs:NCName` (`parseNamedComplexType` via `xmlchar.IsValidNCName`; a colon or leading digit is a schema error, version-INDEPENDENT). KNOWN GAPS (skipped): whitespace in a genuinely-EMPTY (`<xs:sequence/>`, empty model group, or no model group) element-only content type with no effective open content IS rejected in 1.1 (`validate.go` `validateContentByType`: an `ContentTypeElementOnly` type whose model has no content per `modelGroupHasContent` routes to `validateEmptyContent(..., strict=true)`, which rejects ALL character content including whitespace per cvc-complex-type.2.1; fixes open012.n3 — read_types.go still classifies an empty compositor as element-only with a non-nil empty model group, so the emptiness is detected at validation, not classification; 1.0 keeps the whitespace tolerance, byte-identical). KNOWN GAP: an attribute-only `ContentTypeEmpty` complex type still tolerates whitespace-only character content in XSD 1.1 — a pre-existing false-accept, not exercised by the W3C suite; the empty-content whitespace fix landed only for the element-only-misclassified `<xs:sequence/>` case (an empty compositor mis-classified as `ContentTypeElementOnly`), NOT the genuinely `ContentTypeEmpty` classification; schema-component `@id` uniqueness/NCName validity IS enforced (`check_ids.go` `checkSchemaComponentIDs`: a DOM walk over a schema document validating every XSD-namespace element's `id` attribute as a valid xs:ID — NCName after whitespace-collapse — and unique within that document; fixes open038/open039. This is a version-independent XSD rule, so the walk runs in BOTH XSD 1.0 and 1.1. The walk does NOT descend into xs:appinfo/xs:documentation annotation PAYLOAD (arbitrary content, not schema components, so an embedded element's `id` there is not collected) — but an xs:annotation element's OWN `id` participates. Run on EVERY schema document with a FRESH per-document `seen` set — the entry document (`compile.go`) AND each nested document loaded by xs:include/xs:redefine (`compile_imports.go`), xs:import (the import sub-compiler, propagated via `propagateImpErrors`), and xs:override (`override.go`) — after conditional-inclusion pruning, so vc-excluded components' @ids don't count. xs:ID uniqueness is PER-DOCUMENT, so the same value may recur ACROSS documents but not within one));
- **document-wide xs:ID/xs:IDREF/xs:IDREFS validation** (`validate_id.go`: a third validation walk, gated to 1.1, enforcing ID uniqueness and IDREF referential integrity — an ID identifies the element bearing it, so an attribute ID is owned by its element and an element-content ID by its PARENT (an element-content ID on the DOCUMENT ROOT has no parent element, so it denotes NO element — `idOwner` returns nil and `recordID` skips it, so the value never enters the table and any xs:IDREF to it dangles; W3C idIDREF s3_3_4ii26/ii27), and the same value may recur as long as it identifies one element, matching the 1.1 multiple-ID relaxation; list/union values are decomposed to their atomic ID/IDREF leaves via the active-member resolver; element-content collection is SKIPPED when the element has CHILD ELEMENTS (`hasChildElement` — simple content forbids them, so pass 1 already rejected it structurally; `elemTextContent` ignores children and a default/fixed must not stand in for non-empty content, which would otherwise fabricate a spurious duplicate/dangling); a default/fixed value is used only for genuinely-empty content, and NOT on a CONFIRMED nilled element — one DECLARED nillable (`idcHostDecl(elem).Nillable`) carrying `xsi:nil="true"` (a nilled element has no value, so substituting its default/fixed would fabricate a duplicate ID or dangling IDREF). The check is by DECLARATION, not raw `xsi:nil`: a lax no-declaration element with `xsi:nil` is NOT validly nilled, so its real (assessed) content is still collected. Attribute IDs always apply on a nilled element; element typing uses ONLY `assessedElemType` (and attribute typing ONLY `actualAttrType`) — types recorded at genuine pass-1 ASSESSMENT sites — never `actualElemType` (also written, `assessed=false`, for `processContents="skip"`/lax-no-declaration subtrees purely for pass-2 IDC canonicalization), never `actualElemDecl` (written by `recordElemDecl` at the particle-MATCH scan BEFORE content validation, so a matched-but-unassessed child — e.g. an unsatisfied `minOccurs` — would otherwise be misclassified and report a spurious duplicate/dangling), and never a global fallback. The host declaration (`idcHostDecl`, for default/fixed/nillable metadata) is consulted only AFTER an assessed type exists. A `processContents="lax"` element with no declaration but a RESOLVABLE `xsi:type` IS laxly assessed by the shared `assessLaxElement` helper (validated against that type, `assessed=true`) — used for BOTH a directly wildcard-matched lax element (`matchWildcardParticle`) and an xs:anyType/lax descendant (`annotateAnyTypeChildren`) — so its `xsi:type="xs:ID"`/`xs:IDREF` content participates in the ID/IDREF pass. An undeclared element has no nillable declaration, so `xsi:nil="true"` does NOT exempt it from validation: its content is still checked against the governing type (a nilled lax element with invalid/forbidden content is rejected). `skip` content is NEVER assessed, so even one carrying `xsi:type="xs:ID"` is not mis-typed; a STRICT wildcard FAILURE (no global declaration) is likewise NOT assessed — `validateWildcardChild` walks its subtree with `annotateSkipChildren` (canonicalization-only), NOT `annotateAnyTypeChildren` (which would laxly ASSESS globally-declared/xsi:typed descendants of the strict-failed subtree and fabricate a spurious duplicate-ID/dangling on top of the real strict error);
- **identity-constraint @xpathDefaultNamespace** (a LOCALLY-PRESENT value on xs:selector/xs:field is resolved against THAT element's context via `resolveXPathDefaultNSToken`; otherwise the schema-level value is inherited — inheritance happens ONLY when the attribute is ABSENT, detected by PRESENCE (`hasAttr`), so an explicit `xpathDefaultNamespace=""` means "no default element namespace" and does NOT inherit the schema-level default; supporting `##targetNamespace`/`##defaultNamespace`/`##local`/URI, applied via the opt-in `xpath1.Evaluator.DefaultElementNamespace`, which affects only unprefixed ELEMENT name tests, never attributes. The schema-level default (`schemaXPathDefaultNS`) is pre-RESOLVED to a URI against the SCHEMA ROOT at root-read time — crucially so an inherited `##defaultNamespace` uses the ROOT's default namespace, NOT a selector/field that may redeclare `xmlns` — and is PER-document: like elementFormDefault/blockDefault/finalDefault it is saved/set/restored across `xs:include`/`xs:redefine` and set on the import sub-compiler in `compile_imports.go`, so an included/imported schema's IDCs use ITS root's value, not the including/importing schema's); and
- **identity-constraint @ref** (`resolveConstraintRefs` in `compile.go`: a key/unique/keyref using `@ref` reuses a same-kind referenced constraint's selector/fields — and a keyref's refer — adopting its QName identity. The `@ref` form is detected by PRESENCE (`hasAttr`, so a literal `ref=""` is recognized, not silently dropped). A dangling ref, an empty `@ref`, a malformed (non-`xs:QName`, e.g. `:u`) `@ref`/`@refer` — validated via `validateQName` before prefix resolution so a leading colon is not silently resolved as an unprefixed reference — an unbound `@ref` prefix, a kind mismatch, or a `@ref` that also carries name/selector/field/refer is a fatal schema error. The `name`/`refer` companions are detected by PRESENCE (`hasAttr`, so an empty-but-present `name=""`/`refer=""` is still rejected), and `refer` is rejected for EVERY kind, not just keyref).
- **identity-constraint structural rules** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1): `parseIDConstraint` (`read_elements.go`) rejects a constraint `@name` that is not a valid `xs:NCName` (`xmlchar.IsValidNCName`, so `a:b`/`1foo` fail) and a `@refer` on a non-keyref (key/unique); `checkKeyRefRefers` (`compile.go`) rejects a keyref whose `{fields}` cardinality differs from the referenced key/unique's; and `checkIDConstraintPlacement` (`check_idc.go`, a DOM walk run on the entry document and every included/imported/redefined/overridden document) enforces the schema-for-schemas placement/content model that the element-declaration-scoped parser cannot see — an `xs:key`/`xs:unique`/`xs:keyref` is valid ONLY as a child of an `xs:element` declaration (a constraint at the top level or inside `xs:attribute`/`xs:group`/`xs:attributeGroup`/`xs:complexType`/`xs:simpleType`/a model group/an `xs:selector`/`xs:field` is a schema error, otherwise silently dropped), and an `xs:selector`/`xs:field` is valid ONLY as a child of an identity constraint with content model `(annotation?)` and only the `{id?, xpath, xpathDefaultNamespace?}` attributes (a stray non-annotation child, a second annotation, or an unexpected unqualified attribute such as `name` is a schema error). Known deferred IDC gaps (still accepted in 1.0/1.1): a selector/field `@xpath` that violates the restricted IDC-XPath subset grammar (unbound prefix, `.//.//`, `self::node()`, mid-path `//`, whitespace in a QName), and a `fixed` value on a repeating `xs:ID` attribute — the restricted-XPath-grammar validator is not implemented.
- **attribute `use` representation rules** (version-INDEPENDENT — enforced in BOTH XSD 1.0 and 1.1): `checkAttributeUse` (`check_elements.go`) rejects a `@use` on a GLOBAL (top-level) attribute declaration (`isGlobalAttributeDecl` — the schema-for-schemas `topLevelAttribute` type omits `use`), and requires a local attribute use's `@use` to be one of `{optional, prohibited, required}` (any other value, including the empty string, is a schema error). It likewise rejects a `@form` on a GLOBAL (top-level) attribute declaration (`topLevelAttribute` also omits `form`), and requires a local declaration's `@form` to be one of the `formChoice` enumeration `{qualified, unqualified}` (any other value, including the empty string, is a schema error). It also enforces the **`@name` NCName** (version-INDEPENDENT): a non-`ref` `<xs:attribute>` whose `@name` is present but not a valid `xs:NCName` (`xmlchar.IsValidNCName` — empty, a non-name start such as a leading digit, or a colon-bearing QName like `a:b`) is a schema-representation error (§3.2.2). The dedicated `xmlns` reserved-name check takes precedence (`xmlns` is itself a valid NCName), so the two diagnostics do not double-fire.
- **xs:notation structural rules** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1): `checkNotations` (`check_notations.go`, a DOM walk run on the entry document and every included/imported/redefined/overridden document with a FRESH per-document seen set, mirroring `checkSchemaComponentIDs`/`checkIDConstraintPlacement`) enforces the schema-for-schemas §3.14.2 rules that `parseSchemaChildren` — which only records the `@name` of each top-level notation and ignores the rest — cannot see: an `<xs:notation>` is valid ONLY as a child of `xs:schema` or (XSD 1.1) `xs:override` — whose content model admits notation as a wholesale-replacement component — (a notation nested in any other element, e.g. `xs:redefine`, is a schema error); it must carry a `@name` that is a valid `xs:NCName` after whitespace-collapse (missing/empty/`foo:bar`/`-2.5foo` fail); it must carry at least one of `@public`/`@system` (either may be empty — presence via `hasAttr`, not non-emptiness); its content model is `(annotation?)` (at most one `xs:annotation` child, no other element children, no non-whitespace character content); only the unqualified attributes `id`/`name`/`public`/`system` are allowed (a foreign-namespaced attribute is fine, but an XSD-namespaced or unknown-unqualified attribute is an error); and its `@name` must be unique among the document's notation declarations. The walk skips `xs:appinfo`/`xs:documentation` payload. (The separate `checkEnumQNameAndNotation`/`checkNotationOnDeclarations` still enforce that a used `xs:NOTATION` type is enumeration-derived; those are unchanged.)
- **xs:simpleType structural rules** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1): `parseSimpleType` (`read_types.go`) enforces the `xs:simpleType` content model `(annotation?, (restriction | list | union))` — at most one annotation which must be first, exactly one of restriction/list/union, and no other XSD-namespace child (a missing/second derivation, a mis-ordered/duplicate annotation, or a stray XSD element is a schema error; report-and-continue so a valid derivation still parses). `checkSimpleTypeDerivationBody` enforces each derivation body: **restriction** `(annotation?, (simpleType?, facet*))` — annotation first/at-most-one, at most one simpleType and it must precede the facets, `base` attribute XOR nested simpleType, a `base` or a simpleType child required, and a stray XSD child that is not a recognized facet (`isSimpleTypeFacetElement`)/simpleType/annotation rejected (so a non-existent facet like `<xs:whitespace>` lowercase/`<xs:duration>`/`<xs:period>`/`<xs:precision>`/`<xs:scale>`/`<xs:encoding>`, or an `<xs:attribute>`/`<xs:attributeGroup>`/`<xs:anyAttribute>` inside a simpleType restriction, is an error); **list** `(annotation?, simpleType?)` — `itemType` attribute XOR nested simpleType, a non-empty `itemType` or a simpleType child required (empty `itemType=""` rejected), no other element children; **union** `(annotation?, simpleType*)` — a `memberTypes` attribute and/or at least one simpleType child required, annotation first/at-most-one, no other element children. The `@name` rules are SEPARATE from this grammar and also version-INDEPENDENT: a GLOBAL `<xs:simpleType>` `@name` must be a valid `xs:NCName` (`parseNamedSimpleType` via `xmlchar.IsValidNCName`; a colon or leading digit drops the type), and a LOCAL (anonymous/inline) simpleType — one nested in a restriction/list/union, an element, an attribute, or an `xs:alternative` — must NOT carry a `@name` (`parseSimpleType`'s `local` parameter, true for every caller except the top-level `parseNamedSimpleType`). Type-resolution failures (base/itemType/memberType naming a complexType, a list itemType naming a list, restriction of `xs:anyType`) are a further SEPARATE check, not yet implemented.
- **named model group definition content model** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1 per §3.7.2): `parseNamedGroup` (`read_particles.go`) enforces that a `<xs:group name="...">` has content `(annotation?, (all | choice | sequence))` — exactly one model group child (all/choice/sequence), at most one annotation which must PRECEDE the model group, and no other element children (a direct `<xs:element>`/`<xs:complexType>`/`<xs:simpleType>`/`<xs:attribute>`/`<xs:group ref>` is a stray-child schema error). A missing model group, a second model group, or a second/misplaced annotation is likewise a schema error. The single model group is recorded so references still resolve, and the missing-model-group diagnostic is suppressed when a stray/misplaced child already reported a grammar error on the group. (A group *reference* — `<xs:group ref="...">` inside a content model — is a distinct construct with content `(annotation?)`; this rule constrains only the named DEFINITION.)
- **xs:redefine type self-derivation** (version-INDEPENDENT — enforced in BOTH XSD 1.0 and 1.1 per §4.2.3 src-redefine.5): `checkRedefineSelfDerivation` (`compile_imports.go`, called from the `processRedefineOverrides` complexType/simpleType cases) requires a `<simpleType>`/`<complexType>` child of `<xs:redefine>` to have a `<restriction>` (or, for a complexType, `<extension>`) whose resolved `base` names the redefined type ITSELF — self-derivation is present iff the just-parsed replacement's recorded base ref (`c.typeRefs[newType]`) equals its own name `qn`. A base naming a different type, a same-local base in another namespace (e.g. an imported `foo:b-st`), or no derivation at all is a schema error, and the self-reference patch (redirecting the base ref to the original Phase-A type for extension/restriction merge) is skipped. Group/attributeGroup redefine children are NOT covered by this rule (a group without a self-reference is valid iff it validly restricts the original per src-redefine.6.2, and an attributeGroup redefinition needs no self-reference — both a documented gap).
- **built-in FIXED facet restriction** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1): `checkBuiltinFixedFacetRestriction` (`check_facets.go`) rejects a restriction that changes a facet the built-in base fixes — the `collapse` `whiteSpace` and the `explicitTimezone` on the date/time family, AND `fractionDigits=0` on the `xs:integer` family (`value.IsIntegerFamily`: the `xs:decimal` family MINUS `xs:decimal`). A non-zero `fractionDigits` on `xs:integer` or any type derived from it (long/int/short/byte, the (non)positive/(non)negative integers, the unsigned family, positiveInteger) is a schema error; an explicit `fractionDigits="0"` equals the fixed value and is permitted, and `xs:decimal` — which does NOT fix `fractionDigits` — still accepts a non-zero value.
- Additionally, **conditional inclusion** (`conditional_inclusion.go`: a PRE-PASS over the parsed schema tree, run before `parseSchemaChildren` in both 1.0 and 1.1 mode, that prunes any element — with its whole subtree — excluded by its version-control (`vc:`) attributes from `http://www.w3.org/2007/XMLSchema-versioning`. `vc:minVersion`/`vc:maxVersion` keep iff `minVersion <= processorVersion < maxVersion`; `vc:typeAvailable`/`vc:facetAvailable` keep iff EVERY listed QName is available; `vc:typeUnavailable`/`vc:facetUnavailable` keep iff at least one listed QName is UNavailable (so an EMPTY `*Unavailable` is an unconditional exclude, an empty `*Available` a no-op). "Available" is a fixed per-version PROCESSOR-CAPABILITY check against immutable built-in sets (NOT `c.schema.types`, so a schema that happens to declare `{XSD}error` does not make it "available"): an XSD-namespace built-in type for the active version (xs:error/1.1-only types only in 1.1) or an XSD-namespace facet name recognized by the version (`xs:assertion`/`xs:explicitTimezone` are 1.1-only); non-XSD QNames are unavailable. A vc-excluded ROOT `<xs:schema>` makes the whole document empty (used by included docs). The pre-pass also runs on every included/imported/redefined document. Because the pre-pass UNLINKS nodes, the top-level `Compile` keeps the caller's parsed document immutable: it pre-scans for any vc attribute and, only when one is present, prunes a `helium.CopyDoc` clone (URL preserved) instead of the caller's DOM — so `Compile` is idempotent across versions; a vc-free schema keeps the no-copy fast path. Malformed `vc:` values (bad decimal, invalid/unbound-prefix QName) are fatal schema errors ONLY under 1.1; under 1.0 they are tolerated and the condition skipped. Misspelt versioning-namespace attributes (e.g. `vc:minversion`) are foreign attributes with no effect); and
- **wildcard `notNamespace`/`notQName`** (`xs:any`/`xs:anyAttribute`): `readWildcard` (`read_elements.go`) parses `@notNamespace` (an `xs:basicNamespaceList` of anyURI/`##targetNamespace`/`##local`, mutually exclusive with `@namespace`; an EMPTY list `notNamespace=""` is VALID — a `not` constraint with an empty excluded set that admits ALL namespaces, returned as a NON-NIL empty slice so it stays distinct from an ABSENT `@notNamespace`/nil and `wildcardMatches` treats it as "excludes nothing") into `Wildcard.NotNamespace` and `@notQName` (QName/`##defined`/`##definedSibling`) into `Wildcard.NotQName`/`NotQNameDefined`/`NotQNameDefinedSibling` (`schema.go`). Matching honors them everywhere: `wildcardMatches` excludes `NotNamespace`; `wildcardAllowsExpandedName`/`wildcardExcludesName` (`validate_elem.go`) add the name-level `notQName`/`##defined` exclusion (consulting `schema.elements`/`schema.globalAttrs`), used by element content (`matchWildcardParticle`, `matchAll`), attribute (`validateWildcardAttr` call site, `validate.go`), and open content. `##definedSibling` is resolved post-`resolveRefs` by `resolveDefinedSiblings` (`opencontent.go`) into `SiblingNames`. The UPA overlap test (`check_upa.go` `firstSetEntry.wc`, `wildcardsOverlap`→`constraintsIntersect`) and the restriction-subset check (`wildcardConstraintSubset11`, `wildcard_algebra.go`) are 1.1-aware — the latter rejects a derived wildcard that DROPS a `##defined` the base carries, or (for `##definedSibling`) whose resolved `SiblingNames` set is NARROWER than the base's (it iterates `super.SiblingNames` and requires `sub` to exclude each, not just compare the marker bit). `resolveDefinedSiblings` runs INSIDE `resolveRefs` (after group-ref expansion, before the restriction-derivation checks) so those checks see resolved `SiblingNames`, and it visits ALL parsed complex types — named `schema.types` AND inline ANONYMOUS types (iterating `c.typeDefSources` keys, deduped by `*TypeDef` pointer) — so a local element's inline `##definedSibling` wildcard is resolved too. Content-model trees are SHARED across types (group-ref expansion reuses the group's particle slice; type extension embeds the base model-group pointer), so `resolveDefinedSiblings` DEEP-CLONES a type's content model (`cloneModelGroupForSiblings` — fresh ModelGroup/Particle/Wildcard objects, ElementDecls shared read-only) before assigning `SiblingNames`; otherwise one type's resolved sibling set could overwrite another's through the shared `*Wildcard`, making validation NONDETERMINISTIC (map-iteration-order dependent). The clone is taken only for Version11 types whose content model actually carries a `##definedSibling` wildcard (`modelGroupHasDefinedSibling`). The restriction-derivation functions thread `*Schema` (and an `isAttr` flag into `wildcardConstraintSubset`/`wildcardConstraintSubset11`) so EVERY derivation site uses the full `wildcardAllowsExpandedName` (notQName/`##defined`-aware), not a namespace-only test: `wildcardAllowsName` (element-restricts-wildcard); `checkRestrictionAttrs` (`link_refs.go`, a derived CONCRETE attribute vs the base `anyAttribute`, isAttr=true — so a base wildcard excluding it by `notQName`/`##defined` rejects the restriction); and the per-name SUBSET checks in `wildcardConstraintSubset11` (cos-ns-subset — each name `super` disallows must be DISALLOWED by `sub` under the full test, so a derived wildcard's `##defined` can discharge a base wildcard's explicit `notQName="g"` when `g` is globally declared, while the `super.NotQNameDefined && !sub.NotQNameDefined` whole-marker check still requires `sub` to carry `##defined` when `super` excludes every defined name). `##definedSibling` is permitted ONLY on element wildcards (`parseNotQName` rejects it on `xs:anyAttribute`, passed an `isAttr` flag from `readWildcard`). A normalized wildcard algebra (`wildcard_algebra.go`: `wcConstraint` + union/intersection) drives attribute-wildcard UNION (extension, `wildcardUnion`) and INTERSECTION (the type's "complete wildcard" across `xs:attributeGroup` refs — group wildcards captured in `attrGroupWildcards`, intersected in `link_refs.go`). `attrGroupWildcards` is initialized AND merged into the parent in import sub-compilers (`compile_imports.go`; an imported group's `xs:anyAttribute` otherwise panics on a nil map), and the `xs:redefine` override path parses the group's `xs:anyAttribute`, clears the stale base wildcard, intersects a self-reference's original wildcard, and stores the replacement. A group's `xs:anyAttribute` must be the optional FINAL, unique child (enforced in BOTH `parseNamedAttributeGroup` and the redefine override, Version11). `notQName` QName tokens are validated with the shared `internal/xmlchar.IsValidQName` (accepts non-ASCII XML NameChars like the middle dot) and resolved with `resolveNotQName` (NOT the schema-component `resolveQName`): `@notQName` values are QName ACTUAL VALUES (resolve-QName semantics), so an UNPREFIXED token resolves through the in-scope DEFAULT namespace, or to the ABSENT namespace when there is no default — NEVER the schema's `targetNamespace` (a prefixed token uses the in-scope binding, with the same unbound-prefix diagnostic as `resolveQName`). `constraintToWildcard` and its union/intersection callers carry `SiblingNames` through materialization (intersection unions the sibling exclusions; union retains the sibling names neither operand admits) so a materialized base-`all` wildcard union does not drop `##definedSibling` names and silently accept a re-admitting restriction. A materialized union may retain `SiblingNames` even when the `NotQNameDefinedSibling` MARKER is dropped (the marker survives only when BOTH operands carry it — e.g. a `##definedSibling` wildcard unioned with a DISJOINT plain one); those retained names are still first-class exclusions, so `wildcardHas11Fields` counts `len(SiblingNames)>0` as 1.1 state (the subset check is reached, not the namespace-only 1.0 path) and `wildcardConstraintSubset11` checks `super.SiblingNames` UNCONDITIONALLY (regardless of the marker), with the marker-only reject kept solely for the live open-ended case. The intersection carries the STRONGER processContents of its operands (`strongerProcessContents`, strict>lax>skip) so it is ORDER-INDEPENDENT — a strict group is not weakened to skip by a sibling skip group. The 1.1 UNION (`unionWildcards11`) instead takes the SECOND operand's processContents: its only pc-sensitive caller is TYPE EXTENSION, which passes `wildcardUnion(base, derived)`, and per XSD 3.4.2 the extension's effective (complete) attribute wildcard takes the DERIVED wildcard's processContents (so a `strict` derived wildcard extending a `skip` base is not weakened to skip). Union routing is VERSION-aware, not 1.1-field-aware: `wildcardUnion(w1, w2, version)` routes to `unionWildcards11` for EVERY `Version11` union (plus a defensive 1.1-field fallback), keeping the legacy 1.0 case analysis STRICTLY for `Version10` (byte-identical goldens). This matters even for PLAIN namespace wildcards: the 1.0 path keys pc on `w1` and approximates `##other|##local` as `##any`, so without version-aware routing a 1.1 extension `skip`+`strict` would stay skip and an `xs:all` base `##other|##local` union would wrongly admit a target-namespace element. The UNION `{disallowed names}` follows cos-aw-union (§3.10.6.3): a candidate QName is excluded iff NEITHER operand admits it by NAMESPACE-and-explicit-`notQName` (`wildcardAdmitsNameIgnoringDefined`); `##defined`/`##definedSibling` are folded ONLY as whole markers (kept iff BOTH operands carry them) and do NOT make an individual QName disallowed — so a global-attr name one operand excludes via `##defined` but admits by namespace, while the other excludes it explicitly, is still ADMITTED by the union (W3C wild083 `surprise`). `xs:all` may contain wildcards — matched by `matchAll` (gated on `Version11`, so the 1.0 path is byte-identical); restriction handled by `allRestrictsWithWildcards` (`restriction_particle.go`), which enforces per-base-wildcard MIN and MAX cardinality (the MAX bound applied only to base wildcards that EXCLUSIVELY own their namespace, so disjoint base wildcards are not collapsed into one aggregate capacity). CONCRETE derived elements admitted by a base wildcard (not mapped to a base element) participate in that cardinality accounting (combined totals and per-base min/max) keyed on their exact name, so extra concrete elements cannot overload a base wildcard's `maxOccurs` and a required concrete element satisfies a base wildcard's `minOccurs`. A derived wildcard's processContents must be at least as strong as EVERY base wildcard it INTERSECTS (not merely the weakest in the union), so a `skip` derived wildcard cannot restrict a `strict` same-namespace base wildcard just because a disjoint base wildcard is `skip`. A schema declaring a GLOBAL attribute in the XSI namespace is rejected (no-xsi) — this check is in `parseGlobalAttribute` (`read_elements.go`) and gated on `Version11` because the opt-in contract requires 1.0 byte-identical to origin (the pre-existing LOCAL-qualified-attribute XSI check in `check_elements.go` is left untouched for 1.0). All gated on `Version11`; 1.0 drops group wildcards and ignores notNamespace/notQName, unchanged.
- **xs:all relaxations** (Saxon `All.testSet` 62/62; gated on `Version11`, 1.0 byte-identical): the all group's OWN maxOccurs may be 0 or 1 in 1.1 (`check_elements.go` `validateAllOccurs` accepts `{0,1}`; 1.0 requires exactly 1 — mgO001/mgO018); element members may carry `minOccurs`/`maxOccurs>1`/`unbounded` (`check_elements.go` `checkAllElementParticleOccurs` skips the 0/1 rule in 1.1); `xs:all` may contain element WILDCARDS; an `xs:group ref` resolving to an all group may be nested directly inside another `xs:all` with occurrence 1/1 (flattened — `link_refs.go` `checkAllGroupRef`/`groupRefSource.parentCompositor`; a referenced sequence/choice, or occurrence ≠ 1/1, is rejected; an INLINE `<xs:all>` directly inside an `<xs:all>` is also rejected — cos-all-limited still forbids it, only the group-ref form is the relaxed nesting, `read_particles.go`). `matchAll`/`tryMatchAll` (`validate_elem.go`) DISPATCH by version: XSD 1.0 keeps the legacy boolean-`seen[]` matcher (`matchAll10`/`tryMatchAll10`) BYTE-IDENTICAL (a wildcard particle the parser tolerated inside a 1.0 xs:all is never matched and a required one is reported missing); XSD 1.1 uses a FLAT member list (`flattenAllMembers`) with PER-MEMBER occurrence counting (`matchAll11`/`tryMatchAll11`) — element members (matched only when the child is ADMISSIBLY substitutable via `elemMatchesDeclOrSubst`/`allMemberForChild`, honoring block="substitution"/derivation-block and rejecting abstract heads AND abstract substitution members), wildcard members, and flattened nested-all members, all ORDER-INDEPENDENT against each member's min/max. Restriction subsumption of a base `xs:all` is occurrence-COUNTING per BASE MEMBER (`all_subsumption.go` `allRestrictsByCounting`/`memberContributions`, routed from `restriction_particle.go` `groupRestrictsGroup` for Version11 no-wildcard all:all / sequence:all / choice:all): the derived side's contribution is computed PER base-member index (so a choice's branches correlate on the base member — `choice(A1{2},A2{2})` with A1/A2 in base member a's substitution group restricts a{2} — rather than summing distinct derived names), a derived element maps to a base member by name or substitution group (`findBaseAllMember`, using the shared `admissibleSubstitutionMember` predicate — concrete member, head allows substitution, member's EFFECTIVE type substitutable for the head's, not raw Type pointers) with a BUILT-IN-AWARE NameAndTypeOK type-derivation check (`strictBuiltinAwareDerivedFrom`, so e.g. derived `xs:int` validly restricts base `xs:integer`), a non-emitting particle (prohibited wildcard/empty group) contributes nothing, and every unmapped base member must be emptiable. The wildcard cases route to `allRestrictsWithWildcards`, which FLATTENS nested 1/1 all-groups on both sides (`flattenAllParticles`) and SUMS concrete derived elements per base element (multiple subst-group members → one base element) before accounting remaining concrete/wildcard contributions against the base wildcards' cardinality; a nested NON-all derived group (sequence/choice) that survives flattening is NOT decomposed/accounted and is rejected (fail-closed) rather than silently accepted, so it cannot smuggle an out-of-namespace element past a base wildcard. An `xs:all` may be EXTENDED by another `xs:all` (`link_refs.go` extension loop merges the two member sets into a SINGLE all group — both content models must be all groups with the SAME minOccurs; a sequence/choice extending an all, or an all extending a sequence/choice, is rejected; an open-content `interleave` base may not be extended to `suffix` mode). UPA over all members (`check_upa.go` `walkAllBody`) is fed by MULTI-HEAD substitution groups (`substitutionGroup="a b"` registers the member under every head — `read_elements.go`/`compile.go` `buildSubstGroups`, `ElementDecl.SubstitutionGroups`), so an element substitutable for two distinct all members is a cos-nonambig violation. UPA competition is by DECLARATION membership, so an ABSTRACT substitution member STILL competes (XSD 1.1 §3.8.6.4 / bug 4337) even though it can never appear in an instance — `walkTerm` does NOT skip abstract members (instance MATCHING does, via `elemMatchesDeclOrSubst`/`admissibleSubstitutionMember`, which is a separate question). Substitution-group membership is resolved through a TRANSITIVE, cycle-guarded closure (`transitiveSubstClosure`, `validate_elem.go`) that SEPARATES traversal (reachability) from inclusion: an edge is traversed when it is structurally substitutable (`substitutionMemberTypeOK` — head allows substitution, derivation not blocked, effective-type substitutable; ABSTRACT members are traversable) so a concrete descendant behind an abstract intermediate (`h <- abstract m1 <- concrete m2`) is reached; `block="substitution"`/derivation-block PRUNE the subtree. TWO variants differ only in the INCLUSION predicate: `instanceSubstMembers` includes a reached member only when CONCRETE (abstract excluded) — used by runtime matching (`elemMatchesDeclOrSubst`) and restriction subsumption (`findBaseAllMember`); `substitutableMembersFor` (DECLARATION membership — abstract INCLUDED) drives UPA (`walkTerm`) and the byte-identical XSD 1.0 matcher (`matchAll10`/`tryMatchAll10`/`resolveSubstDecl`). Both are transitive (a `h<-m1<-m2` chain is fully walked); the abstract-included vs abstract-excluded split keeps UPA competition (sg-abstract-upa) distinct from instance admissibility. A nilled element with whitespace-only character content is rejected in 1.1 (cvc-elt.3.2.1; 1.0 still tolerates it). all308 (an empty mixed `xs:all` base extended by an all — bug 6202, RESOLVED WONTFIX 2008-11-21, so the schema stays invalid per All Group Limited §3.8.6.2) is correctly REJECTED: an empty *mixed* base all extended by an all is rejected in `link_refs.go`, while an empty *non-mixed* base stays valid.
- **Circular attribute group definitions** are permitted in 1.1 (W3C bug 15795 / attgC010-D015): a DIRECT self-reference (`read_particles.go`) and an INDIRECT cycle (`link_refs.go` `checkCircularAttrGroupRefs`) are silently dropped/cut instead of reported — the back-edge is still CUT so the flatten/expand walks terminate. XSD 1.0 rejects both (src-attribute_group.3), byte-identical.
- **attributeGroup ref resolution** (version-INDEPENDENT — run in BOTH XSD 1.0 and 1.1 per src-resolve / Attribute Group Definition Representation OK 3): `checkAttrGroupRefsResolve` (`link_refs.go`, run in `resolveRefs` before the circular check) rejects every `<xs:attributeGroup ref="...">` — a nested child of a global group (`attrGroupRefChildren`) OR a member of a complex type / derivation body (`attrGroupRefs`) — whose ref does NOT resolve to a globally-declared attribute group: a ref naming a component in a DIFFERENT symbol space (a complexType or a global attribute of the same name), a name declared nowhere, and an empty/absent ref value (the four ref-recording sites in `read_types.go`/`read_particles.go` now record a present-but-empty `ref=""` via `hasAttr`, so it surfaces here instead of being silently ignored). An unprefixed ref that resolves to the schema targetNamespace but whose group comes from a NO-namespace imported schema falls back to the empty-namespace lookup (mirroring the element/type chameleon fallback), so a valid no-NS-import reference is not flagged. A PERMITTED 1.1 circular ref resolves to an EXISTING group and is unaffected. Diagnostics are collected and sorted by (source, line, local) for deterministic output.
- **Particle restriction language-inclusion fallback** (`restriction_subsume.go` `particleLanguageSubset`, gated Version11): the 1.0 syntactic Particle Valid (Restriction) clauses (`restriction_particle.go`) are known-incomplete and reject many restrictions whose derived content-model LANGUAGE is genuinely a subset of the base's (particlesHa161/Hb008/Hb011/Z001/Z028 — "Invalid restriction which becomes valid in XSD 1.1"). When the syntactic check FAILS in 1.1, `checkRestrictionParticles` PROVES `L(derived) ⊆ L(base)` by product simulation of the two content-model automata over the finite alphabet of names the derived model can emit. It is FAIL-CLOSED / SOUND: it accepts only when it can rigorously prove inclusion (with each derived element declaration validly restricting via `elementRestrictsElement` every same-named base declaration — a base wildcard imposes no type constraint), and returns "not proven" (keeping the rejection) for anything it cannot represent — a DERIVED wildcard, an xs:all group, or an over-large occurrence unroll (`subsumeNFAStateCap`/`subsumePairStateCap`/`subsumeUnrollCap`). Element leaves are expanded to their instance-admissible substitution-group members (so a derived subst member restricts a base abstract head), and a base wildcard matches every admitted name (`wildcardAllowsName`). Because it runs only as a fallback AFTER the syntactic check and only ever ACCEPTS, it can never reject more than before; XSD 1.0 keeps the syntactic verdict, byte-identical.
- **Simple-type 1.1 edges**: `finalDefault="extension"` reaches a simple type's `{final}` (the simpleType final-default mask in `read_types.go` adds `FinalExtension`, spec bug 2074), so a simpleContent extension of an extension-final simple type is rejected; a complexContent restriction's child element type may be validly derived from a UNION base element type's (transitive) member (union member substitutability via `isXsiTypeDerivedFromDeclared` in `elementRestrictsElement`); `checkAnyAtomicTypeUsage` rejects `xs:anyAtomicType` as a user-defined simple type's restriction base / list item type / union member type (W3C bug 11103 — it stays valid as an element/attribute/xsi:type type); `checkAnySimpleTypeUsage` rejects RESTRICTING the simple ur-type `xs:anySimpleType` — as a simpleType restriction base / list item type / union member type, AND as a simpleContent complexType RESTRICTION whose effective content simple type (`effectiveContentSimpleType`) is left as `xs:anySimpleType` (an empty/non-narrowing restriction of a base whose content is the ur-type) — per the note in Part 2 §2.4.1 / W3C bug 14559; it stays valid as an element/attribute/xsi:type type and as the base of a simpleContent EXTENSION (a substitution-group head); and an `xs:NOTATION` atomic restriction's `enumeration` values must name a declared `<xs:notation>` (collected into `compiler.notations` and checked in `checkEnumQNameAndNotation`). All gated on Version11, 1.0 byte-identical.
- **xs:pattern regex 1.1 edges** (`internal/xsdregex`, `CompileVersion(pattern, xsd11)` threaded from `read_types.go` as `c.version == Version11`): an unrecognized `\p{Is...}` block name is VALID and matches every character (XSD 1.1 test bug 13670; the negation matches none), instead of a FORX0002 compile error — a character class whose only members are negated-unknown-block contributions (e.g. `[\P{IsFoo}]`) denotes the empty set and `processCharClass` emits a never-matching construct (`[^` all `]`; the complement `[^\P{IsFoo}]` emits match-any) rather than an empty `[]` RE2 cannot parse; and `\c`/`\C` (XML NameChar) admit the full XML 1.1 / XML 1.0 5th-edition combining range U+0300–U+036F, INCLUDING U+0346, via `xmlNameCharRange`. XSD 1.0 (the default `Compile`, plus xpath3's `Translate`/`Validate` and relaxng, which pass xsd11=false) keeps the FORX0002 rejection and the U+0346-carved `\c` range, byte-identical.
- **Particle-restriction occurrence relaxation** (`restriction_particle.go` `occurrenceEmptiableRestriction`): an EMPTIABLE base group particle has an effective minOccurs of 0, so a derived group may narrow a base group{1,1} whose content is emptiable to {0,1} (an emptiable base already accepts the empty sequence, so the derived's zero-occurrences case is within the base language). Wired into the `groupRestrictsGroup` choice:choice case AND the base-all cases — both the XSD 1.1 occurrence-COUNTING fast-path (`bg.Compositor == CompositorAll`, no wildcards, gating `allRestrictsByCounting`) and the all:all switch branch (gating `recurseAll`/`allRestrictsWithWildcards`) — so an emptiable base all{1,1} narrowed to all{0,1} is accepted. The emptiable-base sequence:sequence case is instead handled by the `particleLanguageSubset` language-inclusion fallback. Gated on Version11; XSD 1.0 keeps the strict `rMin >= bMin` check, byte-identical.
- **xs:override** (`override.go`, §4.2.5/§F: WHOLESALE replacement of any top-level component — element/attribute/simpleType/complexType/group/attributeGroup/notation — in the referenced document by an xs:override child of the same (expanded-name, symbol space); simpleType and complexType share ONE type-definition symbol space (over013). An override child that matches NOTHING in the referenced closure is DROPPED, not added (so a dangling reference to it is an error — over026; this is conformance-verified, NOT the literal §4.2.5 "add all children" reading — registering unmatched children makes over026 the ONLY failing case, empirically confirmed); a matched component is SUPPRESSED during load and the override child registered in its place via the normal named-component parsers (notations are not modeled, so a matched notation override is a no-op). The transform is TRANSITIVE: the referenced document's own xs:include/xs:override are re-applied under the SAME cascaded override set, OUTER override winning on a name+symbol-space collision (double override over009); a nested xs:include is treated as an xs:override carrying the active set (indirect chameleon over020), and a chameleon no-targetNamespace target adopts the overriding schema's namespace exactly like include (over018-020). The active override set is BRANCH-LOCAL: a nested xs:override builds a FRESH map (inherited ∪ its own children, outer-precedence, `activeFromEntries`) so its children can NEVER leak into a LATER SIBLING include/override target's suppression set; `overrideLoadTarget` RETURNS the subset of the active set that matched in its closure and each xs:override REGISTERS its own matched children IMMEDIATELY (`registerMatchedChildren`/`registerOverrideChild`) while the DECLARING document's context (form/block/final defaults, xpathDefaultNamespace, base URI, includeFile) is still active — never deferred to an outer context. EXCEPTION — schema-level `@defaultAttributes`: per §4.2.5 a replacement is COPIED INTO the transformed TARGET document, so the TARGET (overridden) document's `@defaultAttributes` (not the overriding document's) governs which implicit default attribute-group ref it records. `overrideLoadTarget` resolves the target root's `@defaultAttributes` (via the side-effect-free `resolveSchemaDefaultAttributes`, shared with `readSchemaDefaultAttributes`), applies it as the active schema default while parsing the target (so SURVIVING target complex types are governed too — save/reset/restore alongside the form defaults) and RETURNS the `schemaDefaultAttrsState`; the owner (`loadOverride`/`overrideProcessNested`) reapplies it around `registerMatchedChildren` so the replacement records the target's implicit ref or none (so an override target lacking `@defaultAttributes` does not inherit the overriding schema's default group — s3_4_2_4ii08). A replacement matched in a NESTED INCLUDED document of the target is registered under THAT included document's per-document @defaultAttributes AND default open content (overrideMatchCtx threads each matched key's declaring-document defaults through overrideLoadTarget/overrideProcessNested; registerMatchedChildren applies the per-key context), not the direct target's. Termination/cycles: override-transformation loads are tracked in a SEPARATE `overrideVisited` set (distinct from plain include `includeVisited`) keyed by (path + active-override-set FINGERPRINT, `overrideActiveFingerprint` — the sorted symbol/name/replacement-element identity), plus the overriding document's `rootKey`. A back-edge to the root, or a re-reach of a document under the SAME active set, terminates WITHOUT re-loading (permissible circular override over023); but the SAME document reached with a DIFFERENT active set is a DISTINCT transformed document and IS loaded again (keying by path alone would OVER-terminate and silently drop a sibling override's replacements — a real collision then surfaces via the duplicate-component check). A disallowed cycle over024 surfaces as a dangling-reference error. A document pulled in by BOTH a plain xs:include/xs:redefine AND an xs:override is a FATAL conflict (`reportOverrideIncludeConflict`, checked in `overrideLoadTarget` and symmetrically in loadInclude/loadRedefine) — the override yields a distinct constituent whose components would collide with the included originals, so it is NOT a silent no-op. A document overridden twice WITHIN one schema document is rejected (over022, per-document target set in processIncludes/overrideProcessNested), and two override children of one xs:override targeting the same component is rejected (over021). xs:import inside a referenced document is processed via the SHARED `processImport` helper (NOT transformed — over025/over029) so it emits the same already-imported / load-failure WARNINGS as a top-level import; override children may reference components imported only by the overriding schema (over029). Gated to Version11; 1.0 ignores xs:override entirely, byte-identical. Override+xs:redefine interplay is not exercised by the suite and processes redefine normally without cascade — a documented gap.)
- **Wildcard Element Declarations Consistent (EDC)**: the STATIC type-table check (`check_element_consistent.go` `checkWildcardElementConsistent`, gated Version11) rejects a content model whose LOCAL element particle and a same-named GLOBAL element declaration reachable through a lax/strict wildcard have inconsistent {type table}s (conditional type assignment). Only the type TABLE is compared — a differing type DEFINITION is permitted (a wildcard intentionally admits differently-typed elements); both-present {type table}s must be EQUIVALENT and the asymmetric empty-vs-present case is flagged (`typeTablesConsistent` reuses `typeTablesEquivalent`, matching the same-name EDC so the same element cannot get different governing tables by path; two present-but-NON-equivalent tables are inconsistent), and a `skip` wildcard imposes no constraint (it never resolves to the global, so `anyWildcardAllows` skips it). The wildcard set examined INCLUDES the type's EFFECTIVE OPEN-CONTENT wildcard (td.OpenContent, threaded into checkWildcardElementConsistent at the call site): interleave open content can move an extra same-name declared child into the open partition and suffix can match a trailing one, where validateWildcardChild governs it via the GLOBAL's CTA type table — so a local element with a {type table} whose name the open-content wildcard re-admits to an inconsistent-type-table global is flagged at compile time (a skip open wildcard is exempt, same as a content-model skip). The STATIC compile-time check SUFFICES — it rejects the inconsistent schema before any instance validation, so the runtime never sees it; the DYNAMIC EDC enforces a DIFFERENT constraint (governing-type DEFINITION substitutability), not type-table equivalence. The DYNAMIC check (`validate_elem.go` `validateWildcardElementConsistent` + `edcLocalDecls`) walks the matched type's BASE chain (`vc.edcType`, set/restored by `validateContentByType`) in addition to the current content model: a restriction that drops a base's local element declaration but admits the name through a wildcard must have its wildcard governing type validly substitutable for the base's local type — else the instance is invalid. Open-content-admitted children get the SAME dynamic EDC check: validateOpenChildren threads the declared content model as the edcScope into matchWildcardParticle (NOT nil), so an interleave-refined declared-name child moved into the open sub-sequence is checked against the same-named local declaration's type just like an ordinary wildcard-particle child.
- **Skip-wildcard IDC selector scoping**: `annotateSkipChildren` records every element inside a `processContents="skip"` subtree in `vc.skipContentNodes` and `evaluateIDC` drops those nodes from the selector node-set, so an un-assessed skip-content element is never constrained by an ancestor `xs:key`/`xs:unique`/`xs:keyref` (the pass-3 ID/IDREF pass already excluded skip content; this fixes pass-2). 1.1 only — the set is empty in 1.0, byte-identical. `resolveOpenContentDefinedSiblings` resolves the `xs:defaultOpenContent` schema-level default `##definedSibling` interleave case.
- **Current remaining 1.1 gaps**:
  - **document-wide xs:ENTITY/xs:ENTITIES value-space validation IS implemented** (1.1): `validate_entity.go` (`validateEntities`, a fourth validation walk gated to 1.1, called from `validate.go` right after `validateIDIDREF`) enforces cvc-id/§3.3.11 — every xs:ENTITY token must name an external general UNPARSED entity declared in the instance's DTD (`doc.GetEntity(tok)` found && `EntityType()==enum.ExternalGeneralUnparsedEntity`). It mirrors `validateIDIDREF`'s structure (`entityFamilyType` like `idFamilyType`; `checkEntityValue` clones `collectIDFromValue`'s list/union/active-member decomposition; the same `elementTypeForID`/`attrTypeForID`, default/fixed substitution, nilled-element, and `hasChildElement` guards) but needs no cross-element table since the DTD entity table is known up front. Fixes saxonData/Id id017-021 (ENTITY/ENTITIES attribute defaults, a union(ENTITY,integer), and ENTITY/ENTITIES element content). 1.0 stays byte-identical.
  - **Element/attribute default/fixed value validity against the declared type at compile time IS implemented** (1.1): `checkAttrUseConstraints` validates attribute uses and `checkElementDeclConstraints` (`link_refs.go`, run in `resolveRefs`) validates element declarations — an EXPLICIT default/fixed is checked against the element's EFFECTIVE declared type (`effectiveDeclType`, so a no-type substitution-group member is validated against its inherited head type; element-only/mixed content is skipped) through the SHARED `validateSimpleContentValue` (the same path the instance value uses: effective content simple type PLUS inherited base-content facets via `validateNestedSimpleContentBases`) on the version-/schema-aware `validateValue`, so an invalid list/union/builtin default — or one that violates an inherited simpleContent base facet — is a fatal schema error (W3C idIDREF s3_3_4si07/si08). Sources are recorded (`elemDeclConstraintSources`) only in Version11 and merged from import sub-compilers, so XSD 1.0 stays byte-identical. The runtime `typeAcceptsValue` (IDC field) and standalone `TypeDef.Validate` remain on the Version10/nil-schema default — a documented Phase-1 gap.

## XSLT 3.0 — Conformance Scope

The xslt3 package targets **Basic XSLT 3.0** conformance (W3C spec Section 27). The spec defines 8 conformance levels; only "Basic XSLT Processor" is required. The remaining 7 are optional features:

| Feature | Status | Notes |
|---------|--------|-------|
| Basic XSLT Processor | **Target** | Core requirement |
| Schema-Awareness | In progress | `xsl:import-schema`, type annotations |
| Serialization | Implemented | xml/html/text output methods |
| Streaming | Implemented | DOM-materialization; XTSE3430 analysis |
| Higher-Order Functions | Implemented | Via xpath3 |
| XPath 3.1 | Implemented | Via xpath3 |
| Dynamic Evaluation | Implemented | `xsl:evaluate` |
| **Compatibility (1.0/2.0)** | **Not planned** | Optional per spec; 122 tests skipped legitimately |

Do NOT implement XSLT 1.0/2.0 backwards compatibility mode. Tests skipped with reason `"unsupported feature: backwards_compatibility"` or `"unsupported spec: XSLT20"` are intentionally out of scope.

## Generated Files

- NEVER modify generated files by hand. Regenerate through the owning generator (e.g. `go generate`).

## Pre-Read Rules

Read the linked doc BEFORE working in that area. No exceptions.

| Trigger | Doc |
|---------|-----|
| Package purpose, API, files | `.claude/docs/packages.md` |
| Cross-package imports | `.claude/docs/dependencies.md` |
| Working with `context.Context`, package `Context` payloads, carrier/accessor patterns | `.claude/docs/context.md` |
| Feature status, test counts, known gaps, ParseOption support | `.claude/docs/libxml2-parity.md` |
| Writing/running tests, golden files, test data, helpers | `.claude/docs/testing.md` |
| Writing/editing `examples/`, example scope, example comments | `.claude/docs/testing.md` |
| Error types, format strings, ErrorHandler, ValidateError | `.claude/docs/error-formatting.md` |
| Parse pipeline, encoding, entities, SAX→DOM, push parser | `.claude/docs/parser-internals.md` |
| DOM node hierarchy, struct fields, namespace/attr storage | `.claude/docs/node-types.md` |
| XSD/RELAX NG/Schematron compile→validate flow | `.claude/docs/validation-pipeline.md` |
| helium CLI commands, flags, pipeline, exit codes | `.claude/docs/helium-command.md` |
| XPath 3.1 design overview, constraints, sub-doc index | `.claude/docs/xpath3-design.md` |
| XPath 3.1 architecture, file layout, internal/xpath | `.claude/docs/xpath3-architecture.md` |
| XPath 3.1 public API, Context, Result, errors | `.claude/docs/xpath3-api.md` |
| XPath 3.1 Item/Sequence/Map/Array type system | `.claude/docs/xpath3-types.md` |
| XPath 3.1 lexer, parser, AST nodes | `.claude/docs/xpath3-parser.md` |
| XPath 3.1 evaluator, comparison, casting | `.claude/docs/xpath3-eval.md` |
| XPath 3.1 function system, built-in categories | `.claude/docs/xpath3-functions.md` |
| Saxon-HE source layout (reference) | `.claude/docs/saxon-layout.md` |

## Cache Maintenance

These docs cache repository state. Still read source before modifying code.

1. When your changes affect a doc below, update it in the same commit.
2. If you notice any doc is wrong or stale — even on an unrelated task — fix it immediately.
3. **Write current state, not history.** These are caches of what the code *is*, not a changelog of how it got there. State what the code does now; never append "was X, now Y", "no longer / previously / used to", "the regression where…", "fixes #NNN", "round-N", or resolved "KNOWN RESIDUAL / deferred" notes — that framing rots into staleness the moment the state changes. When you fix a gap, delete its gap note (don't rewrite it to "now fixed"); when you change behavior, describe the new behavior in the present tense. Keep design rationale (spec citations, version-gating / byte-identical constraints, genuine *current* gaps) — that is current state; the journey belongs in git history.

| Doc | Update trigger |
|-----|----------------|
| `packages.md` | Public API, package, or key file changes |
| `dependencies.md` | Inter-package import changes |
| `context.md` | `context.Context` conventions, package `Context` payload pattern, `NewContext`/`GetContext` guidance changes |
| `libxml2-parity.md` | Test count, parser limitation, feature, or ParseOption changes |
| `testing.md` | Test data layout, helper, env var, or test pattern changes |
| `error-formatting.md` | Error format, error type, or ErrorHandler changes |
| `parser-internals.md` | Parse pipeline, encoding, entity, SAX, or parserCtx changes |
| `node-types.md` | Node type, struct field, or tree operation changes |
| `validation-pipeline.md` | Compile/validate phase, data model, or backtracking changes |
| `helium-command.md` | CLI command, flag, pipeline, or exit code changes |
| `xpath3-design.md` | Design constraints, sub-doc structure changes |
| `xpath3-architecture.md` | Package layout, file additions/removals, import graph changes |
| `xpath3-api.md` | Public API, Context, Result, error type changes |
| `xpath3-types.md` | Item/Sequence/AtomicValue/Map/Array type changes |
| `xpath3-parser.md` | Lexer, parser, AST node, token type changes |
| `xpath3-eval.md` | Evaluator, comparison, casting logic changes |
| `xpath3-functions.md` | Function registry, built-in function additions/changes |
| `saxon-layout.md` | Reference layout updates |
