# enum

The `enum` package defines shared typed enumerations for XML DTD declarations.

Import path: `github.com/lestrrat-go/helium/enum`

This package is primarily a low-level support package used by parser, SAX, and
tree APIs.
